---
phase: 06-notifications
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/ui/notification_manager.py
  - tests/test_notification_manager.py
autonomous: true
requirements:
  - NOTIF-01
  - NOTIF-02
must_haves:
  truths:
    - "NotificationManager.check() fires show_toast exactly once when percent crosses below threshold for the first time"
    - "check() does NOT fire when percent is None, when status is OFFLINE, or when battery is at or above threshold"
    - "check() does NOT fire a second time within the cooldown window after a first alert"
    - "check() DOES fire again after cooldown expires (time-travel test)"
    - "check() clears cooldown entry when state.status is OFFLINE so next ONLINE poll can alert fresh"
    - "Default threshold is 15% when no per-device config entry exists"
  artifacts:
    - path: "src/ui/notification_manager.py"
      provides: "NotificationManager class with __init__ and check() method"
      exports: ["NotificationManager"]
    - path: "tests/test_notification_manager.py"
      provides: "All NOTIF-01 and NOTIF-02 unit tests (no real WinRT calls)"
      contains: "test_fires_on_threshold_crossing"
  key_links:
    - from: "tests/test_notification_manager.py"
      to: "src/ui/notification_manager.py"
      via: "patch('ui.notification_manager.WindowsToaster')"
      pattern: "WindowsToaster"
    - from: "src/ui/notification_manager.py"
      to: "src/monitor/state.py"
      via: "from monitor.state import DeviceState, DeviceStatus"
      pattern: "DeviceStatus\\.OFFLINE"
---

<objective>
Create NotificationManager — the threshold-check and cooldown-tracking service — with a full unit test suite that uses a mock WindowsToaster so no real WinRT calls are made.

Purpose: Deliver the notification logic in isolation before wiring it into the main app. Tests prove NOTIF-01 and NOTIF-02 correctness without hardware.
Output: src/ui/notification_manager.py (service), tests/test_notification_manager.py (7 unit tests).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-notifications/06-RESEARCH.md
@.planning/phases/06-notifications/06-PATTERNS.md

<interfaces>
<!-- Key contracts from existing source files — no codebase exploration needed. -->

From src/monitor/state.py:
```python
class DeviceStatus(enum.Enum):
    ONLINE = enum.auto()
    OFFLINE = enum.auto()
    CHARGING = enum.auto()

@dataclass(frozen=True)
class DeviceState:
    vid: int
    pid: int
    dev_idx: int
    device_name: str
    percent: int | None
    charging: bool
    status: DeviceStatus
```

From src/monitor/registry.py (pattern to follow for in-memory dict keyed by 3-tuple):
```python
self._devices: dict[tuple[int, int, int], DeviceState] = {}
```

From tests/test_registry.py (factory helper pattern):
```python
state = DeviceState(
    vid=0x046D, pid=0x0ABA, dev_idx=0xFF,
    device_name="G Pro X Wireless",
    percent=80, charging=False, status=DeviceStatus.ONLINE,
)
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing tests for NotificationManager</name>
  <files>tests/test_notification_manager.py</files>
  <read_first>
    - .planning/phases/06-notifications/06-RESEARCH.md (§Validation Architecture — Phase Requirements Test Map, §Code Examples)
    - .planning/phases/06-notifications/06-PATTERNS.md (Pattern Assignments — tests/test_notification_manager.py section)
    - tests/test_registry.py (factory helper pattern, lines 37-50)
    - tests/test_ui_settings.py (mocker/patch pattern, lines 1-40)
  </read_first>
  <behavior>
    Write tests/test_notification_manager.py covering all 7 NOTIF-01/02 test cases from RESEARCH.md §Validation Architecture. All tests must FAIL (RED) at this point because notification_manager.py does not yet exist.

    Test 1 — test_fires_on_threshold_crossing: patch WindowsToaster; call check() with percent=10, status=ONLINE, config={"thresholds": {}}; assert show_toast called once.
    Test 2 — test_no_fire_above_threshold: same setup but percent=20 (above default 15); assert show_toast NOT called.
    Test 3 — test_no_fire_when_percent_none: percent=None, status=ONLINE; assert show_toast NOT called.
    Test 4 — test_default_threshold: no per-device config entry; percent=14 (just below 15); assert show_toast called once.
    Test 5 — test_cooldown_suppresses: fire once (percent=10), then call check() again immediately; assert show_toast called exactly once total (second call suppressed).
    Test 6 — test_fires_after_cooldown: plant mgr._last_notified[key] = datetime.now() - timedelta(hours=5); call check() with percent=10; assert show_toast called once (cooldown default 4h has expired).
    Test 7 — test_cooldown_resets_on_offline: fire notification, then call check() with status=OFFLINE; assert _last_notified does NOT contain the key after OFFLINE call.

    Import pattern: `from datetime import datetime, timedelta`, `from unittest.mock import MagicMock, patch`, `import pytest`, `from monitor.state import DeviceState, DeviceStatus`, `from ui.notification_manager import NotificationManager`.
    Factory helper: `_make_state(percent=10, status=DeviceStatus.ONLINE, charging=False)` returning a DeviceState with vid=0x046D, pid=0x0ABA, dev_idx=0xFF, device_name="G Pro X Wireless".
    All test functions that call check() must patch WindowsToaster at "ui.notification_manager.WindowsToaster".
  </behavior>
  <action>Create tests/test_notification_manager.py with 7 test functions using the factory helper and patch context manager. No real WinRT import. Run `pytest tests/test_notification_manager.py -x` — all tests must fail with ImportError or similar because the module does not exist yet.</action>
  <verify>
    <automated>cd F:/Cursor/batteryChecker && python -m pytest tests/test_notification_manager.py -x 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_notification_manager.py exists
    - File contains exactly 7 test functions: test_fires_on_threshold_crossing, test_no_fire_above_threshold, test_no_fire_when_percent_none, test_default_threshold, test_cooldown_suppresses, test_fires_after_cooldown, test_cooldown_resets_on_offline
    - Running `pytest tests/test_notification_manager.py -x` exits non-zero (RED phase — module not yet created)
  </acceptance_criteria>
  <done>7 test functions written and confirmed failing (RED)</done>
</task>

<task type="tdd">
  <name>Task 2: Implement NotificationManager to make all tests pass</name>
  <files>src/ui/notification_manager.py</files>
  <read_first>
    - .planning/phases/06-notifications/06-RESEARCH.md (§Architecture Patterns Pattern 1 and Pattern 2, §Common Pitfalls)
    - .planning/phases/06-notifications/06-PATTERNS.md (Pattern Assignments — src/ui/notification_manager.py section)
    - src/monitor/registry.py (in-memory dict keyed by 3-tuple — follow the same class structure)
    - src/ui/settings_manager.py (module docstring convention "No Qt imports", lines 1-9)
  </read_first>
  <behavior>
    Implement src/ui/notification_manager.py so all 7 tests turn GREEN.

    Module docstring: "Notification dispatch and cooldown tracking for PeriphWatcher.\n\nNo Qt imports — pure stdlib so tests run headlessly.\nInstantiated in __main__.py; check() called from main-thread drain path."

    Imports: `from __future__ import annotations`, `from datetime import datetime`, `from windows_toasts import Toast, WindowsToaster`, `from monitor.state import DeviceState, DeviceStatus`.

    Class NotificationManager:
      - __init__: self._toaster = WindowsToaster("PeriphWatcher"); self._last_notified: dict[tuple[int, int, int], datetime] = {}
      - check(self, state: DeviceState, config: dict) -> None:
          key = (state.vid, state.pid, state.dev_idx)
          if state.status == DeviceStatus.OFFLINE: self._last_notified.pop(key, None); return
          if state.percent is None: return
          thresholds = config.get("thresholds", {})
          device_cfg = thresholds.get(f"{state.vid}:{state.pid}", {})
          threshold = max(1, min(99, int(device_cfg.get("threshold_pct", 15))))
          cooldown_hours = device_cfg.get("cooldown_hours", 4)
          if state.percent >= threshold: return
          last = self._last_notified.get(key)
          if last is not None:
              elapsed = (datetime.now() - last).total_seconds() / 3600
              if elapsed < cooldown_hours: return
          toast = Toast(text_fields=[f"{state.device_name} battery low", f"Battery at {state.percent}% — charge soon"])
          self._toaster.show_toast(toast)
          self._last_notified[key] = datetime.now()

    Threshold clamp max(1, min(99, ...)) satisfies RESEARCH.md §Security Domain input validation requirement.
  </behavior>
  <action>Create src/ui/notification_manager.py with the NotificationManager class as specified. Run `pytest tests/test_notification_manager.py -x` — all 7 tests must pass (GREEN).</action>
  <verify>
    <automated>cd F:/Cursor/batteryChecker && python -m pytest tests/test_notification_manager.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/notification_manager.py exists
    - File contains `class NotificationManager(`
    - File contains `def check(self, state: DeviceState, config: dict) -> None:`
    - File contains `self._last_notified.pop(key, None)` (cooldown reset on OFFLINE)
    - File contains `max(1, min(99,` (threshold clamping)
    - No Qt import anywhere in the file (grep -v "^#" src/ui/notification_manager.py | grep -c "PySide6\|PyQt" returns 0)
    - `pytest tests/test_notification_manager.py -x -v` exits 0 with 7 passed
  </acceptance_criteria>
  <done>All 7 unit tests pass; NotificationManager implements NOTIF-01 and NOTIF-02 logic correctly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.json → NotificationManager | Threshold values from disk file cross into numeric comparison |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | config.json threshold_pct | mitigate | Clamp threshold to [1, 99] via max(1, min(99, int(...))) in NotificationManager.check(); never raise on bad config |
| T-06-02 | DoS | notification spam via rapid config change | accept | Cooldown dict is in-memory and keyed per-device; config reload does not reset cooldown |
| T-06-SC | Tampering | pip install windows-toasts | accept | Package is pre-approved in CLAUDE.md tech stack; legitimacy audit in 06-RESEARCH.md confirms PyPI provenance, GitHub source, 3yr history |
</threat_model>

<verification>
`pytest tests/test_notification_manager.py -x -v` passes 7 tests.
`pytest tests/ -x` (full suite) still passes — no regressions.
</verification>

<success_criteria>
- src/ui/notification_manager.py exists with NotificationManager class
- tests/test_notification_manager.py passes 7 unit tests with zero real WinRT calls
- NOTIF-01: threshold comparison logic present and tested
- NOTIF-02: cooldown suppression and offline-reset logic present and tested
- Full test suite still green (no regressions)
</success_criteria>

<output>
Create .planning/phases/06-notifications/06-01-SUMMARY.md when done
</output>
