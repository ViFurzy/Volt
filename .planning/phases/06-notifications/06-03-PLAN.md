---
phase: 06-notifications
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified: []
autonomous: false
requirements:
  - NOTIF-01
  - NOTIF-02
must_haves:
  truths:
    - "A Windows toast notification is visible in Action Center when battery drops below threshold with a live device connected"
    - "No second toast appears within 4 hours on the same device (cooldown active)"
    - "After dongle unplug and replug with battery below threshold, a fresh toast fires (cooldown cleared on OFFLINE)"
  artifacts: []
  key_links:
    - from: "src/__main__.py"
      to: "Windows Action Center"
      via: "WindowsToaster.show_toast() on Qt main thread drain"
      pattern: "show_toast"
---

<objective>
Hardware verification: confirm that Windows toast notifications appear in Action Center for a real connected device, validate the AUMID path for unpackaged Python apps on Windows 11, and gate NOTIF-01/NOTIF-02 as complete.

Purpose: Unit tests prove logic in isolation; this checkpoint proves the WinRT path works end-to-end on the actual machine without manufacturer software. It also resolves RESEARCH.md Open Question 1 (WindowsToaster vs InteractableWindowsToaster on Windows 11).
Output: NOTIF-01 and NOTIF-02 hardware-verified; fallback AUMID documented if needed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-notifications/06-RESEARCH.md
@.planning/phases/06-notifications/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke-test WindowsToaster on this machine</name>
  <files>src/ui/notification_manager.py (conditionally modified — only if InteractableWindowsToaster fallback is needed)</files>
  <read_first>
    - .planning/phases/06-notifications/06-RESEARCH.md (§Open Questions Q1 — WindowsToaster vs InteractableWindowsToaster on Windows 11, §Common Pitfalls Pitfall 2)
  </read_first>
  <action>
    Run a standalone Python smoke test to confirm WindowsToaster works as an unpackaged app on this Windows 11 machine:

      python -c "
      from windows_toasts import Toast, WindowsToaster
      toaster = WindowsToaster('PeriphWatcher')
      toast = Toast(text_fields=['PeriphWatcher smoke test', 'Notification system working.'])
      toaster.show_toast(toast)
      print('show_toast() returned — check Action Center')
      "

    If a toast appears in Action Center (bottom-right or Action Center panel): WindowsToaster works. Proceed to Task 2.

    If no toast appears (silent failure): try the InteractableWindowsToaster fallback:
      python -c "
      from windows_toasts import Toast, InteractableWindowsToaster
      toaster = InteractableWindowsToaster('PeriphWatcher')
      toast = Toast(text_fields=['PeriphWatcher smoke test', 'Notification system working.'])
      toaster.show_toast(toast)
      print('InteractableWindowsToaster returned — check Action Center')
      "

    If InteractableWindowsToaster works: update src/ui/notification_manager.py to import and use InteractableWindowsToaster instead of WindowsToaster (one-line change in __init__). Document the finding in the SUMMARY.

    If neither works: escalate — record the exact error or silent failure mode in the SUMMARY and flag for investigation before proceeding.
  </action>
  <verify>
    <automated>python -c "from windows_toasts import Toast, WindowsToaster; toaster = WindowsToaster('PeriphWatcher'); toast = Toast(text_fields=['PeriphWatcher test', 'OK']); toaster.show_toast(toast); print('show_toast OK')"</automated>
    <automated>python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `show_toast()` call returns without exception (printed output confirms no exception raised)
    - Either WindowsToaster or InteractableWindowsToaster is confirmed working; choice is documented in SUMMARY
  </acceptance_criteria>
  <done>Toast API confirmed working on this Windows 11 machine; correct toaster class identified</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    NotificationManager is wired end-to-end: Plan 01 added the notification_manager.py service (7 unit tests green), Plan 02 installed windows-toasts, extended settings_manager defaults, and wired NotificationManager.check() into the MonitorApp consumer lambda. Task 1 of this plan confirmed the WinRT toast API fires correctly on this machine.
  </what-built>
  <how-to-verify>
    Step 1 — Temporarily lower threshold for quick test: edit src/ui/settings_manager.py, change `_DEFAULTS` to `{"launch_at_startup": False, "thresholds": {"1133:2746": {"threshold_pct": 90, "cooldown_hours": 0}}}` (threshold 90% so any battery state triggers, cooldown 0 so every poll fires). Note: 1133 is decimal for 0x046D (Logitech VID), 2746 is decimal for 0x0ABA (G Pro X PID).

    Step 2 — Start the app: `python -m src`

    Step 3 — With the Logitech dongle plugged in, observe: within 60 seconds (first poll) a toast notification should appear in the Windows Action Center titled "G Pro X Wireless battery low" with body "Battery at XX% — charge soon".

    Step 4 — Verify NOTIF-02 (cooldown): with cooldown_hours=0 set above, a second toast should appear after the next 60s poll. Change cooldown_hours back to 4, restart app — no second toast should appear within the same session.

    Step 5 — Verify cooldown reset on disconnect: unplug the dongle (device goes OFFLINE); replug (device goes ONLINE); within 60 seconds a fresh toast should appear even though cooldown has not expired.

    Step 6 — Restore _DEFAULTS: revert `_DEFAULTS` back to `{"launch_at_startup": False, "thresholds": {}}` (no device-specific overrides in defaults).

    Expected results:
    - Toast visible in Action Center with correct title "G Pro X Wireless battery low" and body text including the battery percentage
    - Cooldown suppression confirmed (no toast spam at normal 4h cooldown)
    - Unplug/replug triggers fresh alert (cooldown cleared on OFFLINE)
  </how-to-verify>
  <resume-signal>Type "approved" when all three behaviors confirmed, or describe which step failed and what was observed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python process → Windows WinRT toast service | show_toast() crosses process boundary into Windows notification infrastructure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | DoS | Toast spam if cooldown not enforced | accept | Cooldown logic verified by unit tests in Plan 01; hardware checkpoint confirms no spam |
| T-06-SC | Tampering | pip install windows-toasts | accept | Already installed and verified in Plan 02; no new installs in this plan |
</threat_model>

<verification>
Toast appears in Windows Action Center on first below-threshold poll.
Cooldown suppresses repeat toasts within 4h window.
Unplug/replug cycle produces a fresh alert.
`pytest tests/ -x -q` still exits 0 after any fallback changes.
</verification>

<success_criteria>
- Toast notification visible in Action Center for a real device (NOTIF-01 hardware-verified)
- Cooldown suppression confirmed by live test (NOTIF-02 hardware-verified)
- Cooldown-reset-on-offline behavior confirmed by unplug/replug cycle
- Correct toaster class (WindowsToaster or InteractableWindowsToaster) documented in SUMMARY
- _DEFAULTS restored to clean state (no test overrides committed)
</success_criteria>

<output>
Create .planning/phases/06-notifications/06-03-SUMMARY.md when done
</output>
