---
phase: 06-notifications
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - requirements.txt
  - src/ui/settings_manager.py
  - src/__main__.py
autonomous: true
requirements:
  - NOTIF-01
  - NOTIF-02
must_haves:
  truths:
    - "windows-toasts==1.3.1 is importable in the project virtualenv"
    - "settings_manager._DEFAULTS includes 'thresholds': {} so load_config() returns a thresholds key on every call"
    - "NotificationManager.check() is called for every DeviceState that arrives from the background thread drain"
    - "App starts and MonitorApp consumer calls both window.on_device_update and notif_manager.check on each queue drain"
  artifacts:
    - path: "requirements.txt"
      provides: "windows-toasts==1.3.1 dependency declaration"
      contains: "windows-toasts==1.3.1"
    - path: "src/ui/settings_manager.py"
      provides: "Extended _DEFAULTS with thresholds key"
      contains: '"thresholds": {}'
    - path: "src/__main__.py"
      provides: "NotificationManager instantiation and wiring into MonitorApp consumer"
      contains: "NotificationManager"
  key_links:
    - from: "src/__main__.py"
      to: "src/ui/notification_manager.py"
      via: "from ui.notification_manager import NotificationManager"
      pattern: "NotificationManager"
    - from: "src/__main__.py"
      to: "src/ui/settings_manager.py"
      via: "from ui.settings_manager import load_config"
      pattern: "load_config"
---

<objective>
Wire NotificationManager into the running application: install the windows-toasts dependency, extend settings_manager defaults so thresholds config key never raises KeyError, and rewire the MonitorApp consumer in __main__.py so every queue drain calls both the window update and the notification check.

Purpose: Connect the tested notification logic to the live app so real toasts fire when battery drops below threshold.
Output: requirements.txt updated, settings_manager._DEFAULTS extended, __main__.py rewired with NotificationManager.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-notifications/06-RESEARCH.md
@.planning/phases/06-notifications/06-PATTERNS.md
@.planning/phases/06-notifications/06-01-SUMMARY.md

<interfaces>
<!-- Current state of files this plan modifies — read before touching anything. -->

From src/__main__.py (current wiring — lines 38-44):
```python
app_obj = MonitorApp(consumer=window.on_device_update, poll_interval=2.0)
app_obj.start()
hotplug = app_obj.build_hotplug()
timer = app_obj.make_timer()  # noqa: F841 — kept alive intentionally
```

From src/ui/settings_manager.py (current _DEFAULTS — line 23):
```python
_DEFAULTS: dict = {"launch_at_startup": False}
```

From src/ui/notification_manager.py (created in Plan 01):
```python
class NotificationManager:
    def __init__(self) -> None: ...
    def check(self, state: DeviceState, config: dict) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install windows-toasts and extend settings_manager defaults</name>
  <files>requirements.txt, src/ui/settings_manager.py</files>
  <read_first>
    - requirements.txt (current contents — append windows-toasts==1.3.1)
    - src/ui/settings_manager.py (lines 1-52 — _DEFAULTS on line 23 is the only change needed)
    - .planning/phases/06-notifications/06-RESEARCH.md (§Standard Stack, §Common Pitfalls Pitfall 1 re: old winrt package conflict)
    - .planning/phases/06-notifications/06-PATTERNS.md (Pattern Assignments — src/ui/settings_manager.py section)
  </read_first>
  <action>
    Step 1 — Install the package: run `pip install windows-toasts==1.3.1` in the project virtualenv. This auto-installs winrt-runtime~=3.0 and all winrt-Windows.* namespace packages. Do NOT manually install the old monolithic `winrt` package (causes ModuleNotFoundError per RESEARCH.md Pitfall 1).

    Step 2 — Update requirements.txt: append `windows-toasts==1.3.1` as a new line. No other changes.

    Step 3 — Extend _DEFAULTS in src/ui/settings_manager.py: change line 23 from `_DEFAULTS: dict = {"launch_at_startup": False}` to `_DEFAULTS: dict = {"launch_at_startup": False, "thresholds": {}}`. This is the only change to settings_manager.py — the {**_DEFAULTS, **data} merge pattern on line 50 already handles everything else correctly once the key is present in defaults.

    Do NOT change any other line in settings_manager.py. Do NOT change save_config, load_config, battery_color, or winreg functions.
  </action>
  <verify>
    <automated>cd F:/Cursor/batteryChecker && python -c "from windows_toasts import Toast, WindowsToaster; print('OK')" && python -m pytest tests/test_ui_settings.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contains the line `windows-toasts==1.3.1`
    - `python -c "from windows_toasts import Toast, WindowsToaster; print('OK')"` prints OK without error
    - src/ui/settings_manager.py line containing _DEFAULTS reads: `_DEFAULTS: dict = {"launch_at_startup": False, "thresholds": {}}`
    - `pytest tests/test_ui_settings.py -x -q` exits 0 (no regressions from the _DEFAULTS change)
    - `pytest tests/ -x -q` exits 0 (full suite clean)
  </acceptance_criteria>
  <done>windows-toasts importable; load_config() always returns a "thresholds" key; all existing tests still pass</done>
</task>

<task type="auto">
  <name>Task 2: Wire NotificationManager into __main__.py consumer</name>
  <files>src/__main__.py</files>
  <read_first>
    - src/__main__.py (full file — understand current import block and main() function before modifying)
    - .planning/phases/06-notifications/06-PATTERNS.md (Pattern Assignments — src/__main__.py section, lines 132-165)
    - .planning/phases/06-notifications/06-RESEARCH.md (§Architecture Patterns Pattern 4)
    - src/ui/notification_manager.py (NotificationManager signature from Plan 01)
  </read_first>
  <action>
    Modify src/__main__.py with three surgical changes:

    Change 1 — Add two imports to the existing import block (after the existing `from ui.styles import DARK_QSS` line):
      `from ui.notification_manager import NotificationManager`
      `from ui.settings_manager import load_config`
    The load_config import is needed so _on_device_update can call it. Do not add it inside the function body.

    Change 2 — Add a module-level helper function _on_device_update before main():
      `def _on_device_update(window: MainWindow, notif_manager: NotificationManager, state: DeviceState) -> None:`
      `    window.on_device_update(state)`
      `    cfg = load_config()`
      `    notif_manager.check(state, cfg)`
    Add the `from monitor.state import DeviceState` import if it is not already present (check the file first).

    Change 3 — In main(), replace the single line:
      `app_obj = MonitorApp(consumer=window.on_device_update, poll_interval=2.0)`
    with:
      `notif_manager = NotificationManager()`
      `app_obj = MonitorApp(consumer=lambda s: _on_device_update(window, notif_manager, s), poll_interval=2.0)`

    Do NOT move, remove, or modify any other line: sys.coinit_flags = 0 stays as the very first line; sigint_timer, hotplug, timer references remain unchanged.

    The `sys.coinit_flags = 0` guard on line 1 is the architecture invariant — it must remain the first executable statement. Verify this is still true after your edits.
  </action>
  <verify>
    <automated>cd F:/Cursor/batteryChecker && python -c "import ast, sys; src = open('src/__main__.py').read(); tree = ast.parse(src); first = tree.body[0]; print('coinit guard OK' if (hasattr(first,'value') and 'coinit_flags' in ast.unparse(first)) else 'FAIL: coinit_flags not first')" && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/__main__.py line 1 still contains `sys.coinit_flags = 0` (architecture invariant — verified by AST parse or grep)
    - src/__main__.py contains `from ui.notification_manager import NotificationManager`
    - src/__main__.py contains `from ui.settings_manager import load_config`
    - src/__main__.py contains `def _on_device_update(`
    - src/__main__.py contains `notif_manager = NotificationManager()`
    - src/__main__.py contains `lambda s: _on_device_update(window, notif_manager, s)`
    - `pytest tests/ -x -q` exits 0 (full suite green)
  </acceptance_criteria>
  <done>NotificationManager is instantiated in main() and called on every queue drain; full test suite still passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip install → virtualenv | Package install from PyPI crosses trust boundary |
| config.json → load_config → check() | Disk-persisted thresholds read into memory each drain cycle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | pip install windows-toasts | accept | Package pre-approved in CLAUDE.md; legitimacy audit in RESEARCH.md §Package Legitimacy Audit |
| T-06-04 | Tampering | config.json thresholds values | mitigate | Already mitigated in Plan 01: max(1, min(99, int(...))) clamp in NotificationManager.check() |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | windows-toasts==1.3.1 verified [Approved] in RESEARCH.md §Package Legitimacy Audit |
</threat_model>

<verification>
`python -c "from windows_toasts import Toast, WindowsToaster; print('OK')"` prints OK.
`pytest tests/ -x -q` exits 0 (all tests including pre-existing suites).
src/__main__.py line 1 is still `sys.coinit_flags = 0`.
</verification>

<success_criteria>
- windows-toasts==1.3.1 installed and importable
- requirements.txt updated with windows-toasts==1.3.1
- settings_manager._DEFAULTS has "thresholds": {}
- __main__.py wires NotificationManager.check() into every queue drain cycle
- sys.coinit_flags = 0 remains first line of __main__.py (architecture invariant intact)
- Full test suite (pytest tests/ -x) still green
</success_criteria>

<output>
Create .planning/phases/06-notifications/06-02-SUMMARY.md when done
</output>
