---
phase: 07-bluetooth-device-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified: []
autonomous: false
requirements:
  - BT-01

must_haves:
  truths:
    - "We know whether BATTERY_PKEY returns a non-None value from DeviceInformation.find_all_async() with default DeviceInformationKind"
    - "We know whether classic BT and BLE devices both appear in the AQS-filtered enumeration"
    - "The bt_backend.py implementation is adjusted (if needed) based on hardware findings before service integration"
  artifacts:
    - path: ".planning/phases/07-bluetooth-device-discovery/07-02-SUMMARY.md"
      provides: "Hardware findings: PKEY behavior, enumeration completeness, any required adjustments"
  key_links:
    - from: "src/monitor/bt_backend.py"
      to: "Windows BT device properties"
      via: "winrt_enumerate_bt() live run against real hardware"
      pattern: "BATTERY_PKEY"
---

<objective>
Run `winrt_enumerate_bt()` against real paired Bluetooth hardware and validate the WinRT battery PKEY assumption before it is wired into MonitorService.

Purpose: The RESEARCH.md flagged BATTERY_PKEY `{104EA319-6EE2-4701-BD47-8DDBF425BBE5} 2` as [ASSUMED] — it is community-verified but not in official Microsoft docs. If it returns None for all devices, the bt_backend.py implementation needs adjustment before Plan 07-03 wires it into the polling loop. This checkpoint cannot be skipped.

Output: Human-verified knowledge of PKEY behavior. Any required adjustments to bt_backend.py before proceeding. SUMMARY with findings.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-bluetooth-device-discovery/07-RESEARCH.md
@.planning/phases/07-bluetooth-device-discovery/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and run hardware probe script</name>
  <files>scripts/probe_bt_devices.py</files>
  <read_first>
    - src/monitor/bt_backend.py (winrt_enumerate_bt implementation from Plan 07-01)
    - .planning/phases/07-bluetooth-device-discovery/07-RESEARCH.md (Open Question 1, Pitfall 2, Pitfall 3)
  </read_first>
  <action>
    Create scripts/probe_bt_devices.py — a standalone async script that:

    1. Calls winrt_enumerate_bt() and prints each discovered device in this format:
         Device: {name}
           id:      {id}
           battery: {battery}  (None if absent)
           type:    {type}

    2. For each device where battery is None AND name contains any keyword that suggests it reports battery (controller, headset, mouse, keyboard), additionally run a second lookup using DeviceInformationKind.Device (import from winrt.windows.devices.enumeration) to check if the PKEY appears on the Device kind instead of the default DeviceInterface kind. Print the result labeled "Device-kind lookup: {value}".

    3. For any BLE device (ble_address available), call gatt_battery(ble_address) and print the GATT result.

    Entry point:
      if __name__ == "__main__":
          import asyncio
          import sys, os
          sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'src'))
          asyncio.run(main())

    Run the script:
      python scripts/probe_bt_devices.py

    Print the complete output to the terminal.
  </action>
  <verify>
    <automated>python scripts/probe_bt_devices.py</automated>
  </verify>
  <done>Script runs without import error; output printed to terminal showing at least one paired BT device (or empty list if none paired)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 07-01 implemented bt_backend.py with winrt_enumerate_bt() using BATTERY_PKEY.
    Task 1 ran the probe script against real paired BT hardware and printed device enumeration results.
  </what-built>
  <how-to-verify>
    Review the probe_bt_devices.py output from Task 1. Answer these questions:

    1. DEVICE ENUMERATION: Did find_all_async() return any devices?
       - If YES: list the device names shown.
       - If NO (empty list): note whether you have paired BT devices in Windows Settings > Bluetooth.

    2. BATTERY PKEY: For each device, was the battery field non-None?
       - YES (non-None for at least one device) → WinRT tier (a) works as expected.
       - NO (all None) → tier (a) always falls through; note if Device-kind lookup showed a value.

    3. DEVICE KINDS: Did the "Device-kind lookup" row show a non-None value for any device?
       - If YES: bt_backend.py needs a second lookup with DeviceInformationKind.Device.
       - If NO: tier (a) is not viable via either kind for this hardware.

    4. STADIA (if paired): Was a "Stadia" controller entry visible in the output?
       - YES → AQS selector covers classic BT as expected.
       - NO → note (may not be paired on this machine).

    Based on findings, approve one of these outcomes:

    OUTCOME A — "PKEY works": battery is non-None for at least one device with default DeviceInformationKind.
    OUTCOME B — "Device-kind needed": battery is None with default kind but non-None with DeviceInformationKind.Device.
    OUTCOME C — "PKEY not supported": battery is None for all devices with both kinds. All devices will fall through to GATT or "battery unknown".

    Type one of: "OUTCOME A", "OUTCOME B", or "OUTCOME C" to proceed.
    If OUTCOME B, Claude will update bt_backend.py before continuing.
  </how-to-verify>
  <resume-signal>Type "OUTCOME A", "OUTCOME B", or "OUTCOME C" with any additional notes about what devices were enumerated.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Adjust bt_backend.py based on hardware findings (if needed)</name>
  <files>src/monitor/bt_backend.py</files>
  <read_first>
    - src/monitor/bt_backend.py (current implementation)
    - .planning/phases/07-bluetooth-device-discovery/07-RESEARCH.md (Pitfall 3, Open Question 1)
  </read_first>
  <action>
    Based on the checkpoint outcome:

    OUTCOME A — No change needed. Confirm the existing implementation is correct.

    OUTCOME B — Update winrt_enumerate_bt() in bt_backend.py to perform a second lookup with DeviceInformationKind.Device for devices where the battery property is None in the default kind:
      - Import DeviceInformationKind from winrt.windows.devices.enumeration at function body level
      - After the first find_all_async call, for each device where battery_pct is None:
        - secondary = await DeviceInformation.find_all_async(aqs, additional_props, DeviceInformationKind.Device)
        - Match by device id; if matched device has battery_raw that is numeric, use it
      - This is a best-effort enhancement; if second lookup fails or still None, continue returning None

    OUTCOME C — No code change needed (PKEY already returns None gracefully; tier (b) GATT handles BLE devices; classic BT devices show "battery unknown"). Add a comment to bt_backend.py noting the hardware finding.

    For OUTCOME B only, update tests/test_bt_backend.py:
    - Add test_device_kind_fallback_lookup: tests that when first-kind result has battery=None, the second DeviceInformationKind.Device lookup is attempted and its result used when non-None.

    Re-run the test suite after any code changes:
      python -m pytest tests/test_bt_backend.py -x -q
  </action>
  <verify>
    <automated>python -m pytest tests/test_bt_backend.py -x -q</automated>
  </verify>
  <done>bt_backend.py reflects the actual hardware-validated behavior; all tests pass; a code comment documents the PKEY hardware finding</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe script output | Hardware-specific; output is informational, not persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Information Disclosure | probe_bt_devices.py output | accept | Script is dev-only; prints to terminal, not persisted; no credentials involved |
</threat_model>

<verification>
After this checkpoint plan:
- Hardware behavior of BATTERY_PKEY is documented in SUMMARY
- bt_backend.py is adjusted to match actual hardware behavior (if OUTCOME B)
- python -m pytest tests/test_bt_backend.py -x -q passes
</verification>

<success_criteria>
- probe_bt_devices.py ran and produced output
- Hardware checkpoint completed with OUTCOME A, B, or C documented
- bt_backend.py updated if OUTCOME B (Device-kind fallback)
- All tests/test_bt_backend.py tests remain green
- SUMMARY records the PKEY findings for Plan 07-03 to rely on
</success_criteria>

<output>
Create `.planning/phases/07-bluetooth-device-discovery/07-02-SUMMARY.md` when done.
Include PKEY outcome (A/B/C), devices enumerated, and any bt_backend.py changes made.
</output>
