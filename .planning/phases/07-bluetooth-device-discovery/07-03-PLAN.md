---
phase: 07-bluetooth-device-discovery
plan: 03
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - src/monitor/state.py
  - src/ui/settings_manager.py
  - src/monitor/service.py
  - tests/test_service.py
autonomous: true
requirements:
  - BT-03
  - BT-04

must_haves:
  truths:
    - "BtDeviceInfo dataclass exists in state.py with bt_id, name, battery, ble_address, status fields"
    - "settings_manager._DEFAULTS contains monitored_devices: [] — backward-compatible"
    - "MonitorService.scan_bt_devices() coroutine runs winrt_enumerate_bt() AND hid.enumerate() and puts merged results on _ui_queue"
    - "MonitorService.poll_once() resolves battery for persisted BT devices from config on each 60s tick"
    - "MonitorService stores discovered BT devices in self._bt_devices dict keyed by bt_id"
    - "New test test_discover_loads_persisted_bt_devices passes in tests/test_service.py"
  artifacts:
    - path: "src/monitor/state.py"
      provides: "BtDeviceInfo dataclass + BtScanResultEvent dataclass"
      contains: "class BtDeviceInfo"
    - path: "src/ui/settings_manager.py"
      provides: "_DEFAULTS with monitored_devices key"
      contains: "monitored_devices"
    - path: "src/monitor/service.py"
      provides: "scan_bt_devices() coroutine, _bt_devices dict, BT branch in poll_once()"
  key_links:
    - from: "src/monitor/service.py"
      to: "src/monitor/bt_backend.py"
      via: "winrt_enumerate_bt() and resolve_battery() calls"
      pattern: "bt_backend\\.winrt_enumerate_bt"
    - from: "src/monitor/service.py"
      to: "queue.Queue"
      via: "_ui_queue.put(BtScanResultEvent(...))"
      pattern: "BtScanResultEvent"
    - from: "src/monitor/service.py"
      to: "src/ui/settings_manager.py"
      via: "load_config()['monitored_devices'] in discover()"
      pattern: "monitored_devices"
---

<objective>
Extend the data model, config schema, and MonitorService to support Bluetooth device tracking, on-demand scanning, and per-poll battery refresh of user-persisted BT devices.

Purpose: This plan wires bt_backend.py into the existing service architecture so BT devices receive the same polling treatment as HID devices. The Devices page (Plan 07-04) depends on the queue event type and scan_bt_devices() API defined here.

Output: `BtDeviceInfo` + `BtScanResultEvent` in state.py, `monitored_devices` default in settings_manager, `scan_bt_devices()` and BT polling in service.py, new tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-bluetooth-device-discovery/07-RESEARCH.md
@.planning/phases/07-bluetooth-device-discovery/07-PATTERNS.md
@.planning/phases/07-bluetooth-device-discovery/07-01-SUMMARY.md
@.planning/phases/07-bluetooth-device-discovery/07-02-SUMMARY.md

<interfaces>
<!-- Existing contracts the executor must replicate/extend. -->

From src/monitor/state.py (current):
```python
@dataclass(frozen=True)
class DeviceState:
    vid: int; pid: int; dev_idx: int; device_name: str
    percent: int | None; charging: bool; status: DeviceStatus

KNOWN_DEVICES: dict[tuple[int, int], str] = {
    (0x046D, 0x0ABA): "G Pro X Wireless",
    (0x1038, 0x1852): "Aerox 5 Wireless",
}
```

From src/monitor/service.py (current):
```python
class MonitorService:
    def __init__(self, ui_queue, registry, poll_interval=60.0): ...
    def rescan(self) -> concurrent.futures.Future:
        return asyncio.run_coroutine_threadsafe(self.discover(), self._loop)
    async def discover(self) -> None: ...   # extend here for BT
    async def poll_once(self) -> None: ...  # extend here for BT
    self._open: dict[tuple[int, int, int], object] = {}   # HID handles
```

From src/ui/settings_manager.py (current):
```python
_DEFAULTS: dict = {"launch_at_startup": False, "thresholds": {}, "close_behavior": None}
# Phase 6 added: "cooldown_hours": 4 — may already be present; check before adding
```

From src/monitor/bt_backend.py (Plan 07-01 output):
```python
async def winrt_enumerate_bt() -> list[dict]:
    # returns list of {"id": str, "name": str, "battery": int|None, "type": "bt"}

async def resolve_battery(device_info: dict) -> int | None:
    # device_info needs keys: "battery" (int|None), "ble_address" (str|None)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add BtDeviceInfo and BtScanResultEvent to state.py; extend settings_manager</name>
  <files>src/monitor/state.py, src/ui/settings_manager.py</files>
  <read_first>
    - src/monitor/state.py (full file — replicate frozen=True dataclass style)
    - src/ui/settings_manager.py (full file — _DEFAULTS dict, check if cooldown_hours already present)
    - .planning/phases/07-bluetooth-device-discovery/07-PATTERNS.md (state.py section — BtDeviceInfo fields, dict pattern)
  </read_first>
  <behavior>
    No dedicated test file for this task — acceptance is structural (dataclass exists, import works, config merge works). Verify via:
      python -c "from monitor.state import BtDeviceInfo, BtScanResultEvent; print('OK')"
      python -c "from ui.settings_manager import load_config; c = load_config(); assert 'monitored_devices' in c; print('OK')"
  </behavior>
  <action>
    In src/monitor/state.py, add two new frozen dataclasses after the existing DeviceState definition:

    BtDeviceInfo:
    - Fields: bt_id: str, name: str, battery: int | None, ble_address: str | None, status: DeviceStatus
    - bt_id is the WinRT DeviceInformation.id string; stable across reboots; used as registry key
    - ble_address is None for classic BT (BR/EDR); set for BLE devices
    - status is DeviceStatus.ONLINE when battery was resolved; DeviceStatus.OFFLINE when resolve returned None
    - Docstring: "Snapshot of a discovered Bluetooth device (BT-01). bt_id is the WinRT DeviceInformation.id string (stable across reboots). ble_address is None for classic Bluetooth (BR/EDR) devices. battery is None when no tier resolved a value."
    - Follow exact frozen=True, docstring, field order convention from DeviceState

    BtScanResultEvent:
    - Fields: devices: list (type annotation: list[dict]) — merged list from winrt_enumerate_bt() + hid.enumerate() (BT-03)
    - Docstring: "Event put on _ui_queue when a BT scan completes (BT-03). Consumed by MonitorApp.drain() → DevicesPage.on_scan_result(). devices contains both BT entries (type='bt') from winrt_enumerate_bt() and HID entries (type='hid') from hid.enumerate()."
    - frozen=True, use from __future__ import annotations at top of file if not already present

    In src/ui/settings_manager.py, extend _DEFAULTS:
    - Check if cooldown_hours is already in _DEFAULTS (Phase 6 may have added it); if absent add it as 4
    - Add monitored_devices: [] as a new key
    - Final _DEFAULTS: {"launch_at_startup": False, "thresholds": {}, "close_behavior": None, "cooldown_hours": 4, "monitored_devices": []}
    - The existing {**_DEFAULTS, **data} merge in load_config() handles backward compatibility — no other changes needed
  </action>
  <verify>
    <automated>python -c "from monitor.state import BtDeviceInfo, BtScanResultEvent, DeviceStatus; b = BtDeviceInfo(bt_id='x', name='Test', battery=75, ble_address=None, status=DeviceStatus.ONLINE); print(b)"</automated>
  </verify>
  <done>BtDeviceInfo and BtScanResultEvent are importable from monitor.state; load_config() returns a dict with 'monitored_devices' key defaulting to []; python -m pytest tests/ -x -q green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend MonitorService with scan_bt_devices() and BT poll_once() branch</name>
  <files>src/monitor/service.py, tests/test_service.py</files>
  <read_first>
    - src/monitor/service.py (full file — discover() structure lines 135-226, poll_once() structure lines 228-299, rescan() pattern lines 107-113, _open dict pattern lines 63-67)
    - .planning/phases/07-bluetooth-device-discovery/07-PATTERNS.md (service.py section — _bt_devices dict, discover() extension, poll_once() extension, rescan() analog)
    - tests/test_service.py (full file — asyncio.run() test pattern, mocker.patch namespace, class grouping)
    - src/monitor/bt_backend.py (winrt_enumerate_bt and resolve_battery signatures)
    - src/monitor/state.py (BtDeviceInfo, BtScanResultEvent — just added in Task 1)
  </read_first>
  <behavior>
    Write the new tests first (RED), then implement (GREEN).

    New tests to add to tests/test_service.py (add as new class TestBtDevices at end of file):

    - test_scan_bt_devices_puts_scan_result_on_queue:
      Patch "monitor.service.bt_backend.winrt_enumerate_bt" to return [{"id": "dev://1", "name": "Stadia", "battery": 82, "type": "bt"}].
      Patch "monitor.service.hid.enumerate" to return [] (no HID devices in this test).
      service = make_service(); asyncio.run(service._run_bt_scan())
      assert not service._ui_queue.empty()
      event = service._ui_queue.get_nowait()
      assert isinstance(event, BtScanResultEvent)
      assert event.devices[0]["name"] == "Stadia"

    - test_scan_bt_devices_includes_hid_entries:
      Patch "monitor.service.bt_backend.winrt_enumerate_bt" to return [].
      Patch "monitor.service.hid.enumerate" to return [
          {"product_string": "G502 X Plus", "vendor_id": 0x046D, "product_id": 0xC098,
           "usage_page": 0xFF00, "path": b"hid_path"}
      ].
      asyncio.run(service._run_bt_scan())
      event = service._ui_queue.get_nowait()
      assert isinstance(event, BtScanResultEvent)
      hid_entries = [d for d in event.devices if d["type"] == "hid"]
      assert len(hid_entries) == 1
      assert hid_entries[0]["name"] == "G502 X Plus"

    - test_scan_bt_devices_stores_in_bt_devices_dict:
      Patch "monitor.service.bt_backend.winrt_enumerate_bt" to return [{"id": "dev://1", "name": "Stadia", "battery": 82, "type": "bt"}].
      Patch "monitor.service.hid.enumerate" to return [].
      asyncio.run(service._run_bt_scan())
      assert "dev://1" in service._bt_devices
      info = service._bt_devices["dev://1"]
      assert isinstance(info, BtDeviceInfo)
      assert info.name == "Stadia"
      assert info.battery == 82

    - test_poll_once_refreshes_persisted_bt_device:
      Patch "monitor.service.bt_backend.resolve_battery" to return 60.
      Pre-populate service._bt_devices with one BtDeviceInfo(bt_id="dev://1", name="Stadia", battery=None, ble_address=None, status=DeviceStatus.ONLINE)
      asyncio.run(service.poll_once())
      event = service._ui_queue.get_nowait()
      assert isinstance(event, BtDeviceInfo)
      assert event.battery == 60

    - test_discover_loads_persisted_bt_devices_from_config:
      Patch load_config to return {"monitored_devices": [{"id": "dev://1", "name": "Stadia", "type": "bt", "ble_address": None}]}.
      Patch "monitor.service.bt_backend.winrt_enumerate_bt" to return [] (no live scan result).
      asyncio.run(service.discover())
      assert "dev://1" in service._bt_devices

    Implementation changes to src/monitor/service.py:

    Imports to add at top (after existing imports):
      import hid
      import monitor.bt_backend as bt_backend
      from monitor.state import BtDeviceInfo, BtScanResultEvent
      from ui.settings_manager import load_config

    In __init__, add after self._voltage_history line:
      # Discovered BT devices keyed by bt_id str.
      # Accessed exclusively on the bg asyncio thread.
      self._bt_devices: dict[str, BtDeviceInfo] = {}

    New public method scan_bt_devices() (same signature as rescan() — thread-safe entry point for Qt main thread):
      def scan_bt_devices(self) -> concurrent.futures.Future:
          """Thread-safe entry point for the Devices page scan button (BT-03).

          Schedules _run_bt_scan() on the bg loop; returns the Future so the caller
          can attach a done callback. Result arrives via _ui_queue as BtScanResultEvent.
          """
          return asyncio.run_coroutine_threadsafe(self._run_bt_scan(), self._loop)

    New coroutine _run_bt_scan() — runs winrt_enumerate_bt() AND hid.enumerate() and merges into BtScanResultEvent.devices (BT-03):
      async def _run_bt_scan(self) -> None:
          # Tier 1: WinRT paired BT devices (classic BT + BLE)
          bt_devices = await bt_backend.winrt_enumerate_bt()
          for d in bt_devices:
              info = BtDeviceInfo(
                  bt_id=d["id"], name=d["name"], battery=d.get("battery"),
                  ble_address=d.get("ble_address"), status=DeviceStatus.ONLINE,
              )
              self._bt_devices[d["id"]] = info

          # Tier 2: Connected HID devices via hid.enumerate() (BT-03 requirement).
          # product_string may be empty for BT HID devices on Windows; use it only when non-empty.
          hid_entries = []
          for info in hid.enumerate():
              name = info.get("product_string") or info.get("manufacturer_string") or "HID Device"
              hid_entries.append({
                  "id": info.get("path", b"").decode("utf-8", errors="replace"),
                  "name": name,
                  "battery": None,  # hid.enumerate() does not expose battery
                  "type": "hid",
              })

          all_devices = bt_devices + hid_entries
          self._ui_queue.put(BtScanResultEvent(devices=all_devices))

    In discover(), add after the SteelSeries block (after line 226) a new block:
      # Load persisted BT devices from config and register them for polling (BT-04).
      # These don't get opened like HID devices; they are polled via resolve_battery().
      cfg_devices = load_config().get("monitored_devices", [])
      for entry in cfg_devices:
          bt_id = entry.get("id")
          if bt_id and bt_id not in self._bt_devices:
              self._bt_devices[bt_id] = BtDeviceInfo(
                  bt_id=bt_id, name=entry.get("name", "Unknown"),
                  battery=None, ble_address=entry.get("ble_address"),
                  status=DeviceStatus.ONLINE,
              )

    In poll_once(), add a new loop after the existing for key, handle loop:
      # BT device battery refresh (BT-04): re-run resolve_battery() for each tracked BT device.
      # Pass bt_info.battery (the WinRT cached value from the last scan) as the "battery" key so
      # that resolve_battery() tier (a) can short-circuit when WinRT already provided a value.
      # bt_info.battery is None when no scan has occurred yet (device just started), in which case
      # tier (a) correctly falls through to tier (b) GATT.
      for bt_id, bt_info in list(self._bt_devices.items()):
          device_info = {"battery": bt_info.battery, "ble_address": bt_info.ble_address}
          battery = await bt_backend.resolve_battery(device_info)
          updated = BtDeviceInfo(
              bt_id=bt_id, name=bt_info.name, battery=battery,
              ble_address=bt_info.ble_address, status=DeviceStatus.ONLINE,
          )
          self._bt_devices[bt_id] = updated
          self._ui_queue.put(updated)

    Note: BT devices always stay ONLINE in this model. "Battery unknown" is represented by battery=None; OFFLINE is not used for BT (device may be on but not exposing battery). The Dashboard card for BT devices will show "--" for None battery, which is correct.
    Note: HID entries in BtScanResultEvent have type='hid' and battery=None. DevicesPage.on_scan_result() handles both types.
  </action>
  <verify>
    <automated>python -m pytest tests/test_service.py -x -q -k "TestBtDevices"</automated>
  </verify>
  <done>5 new tests in TestBtDevices pass (including test_scan_bt_devices_includes_hid_entries); scan_bt_devices() schedules correctly; _run_bt_scan() merges BT and HID entries into BtScanResultEvent.devices; _bt_devices populated from winrt scan and from persisted config; poll_once() refreshes BT battery; resolve_battery() is called with battery=bt_info.battery (not hardcoded None) in the poll_once BT loop; python -m pytest tests/ -x -q green (full suite)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file → service.py | load_config()["monitored_devices"] list from disk — entries may have missing keys |
| bt_backend → service.py | winrt_enumerate_bt() returns None-guarded dicts; safe |
| hid.enumerate() → service.py | path bytes may contain non-UTF-8; product_string may be empty |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | monitored_devices config entries | mitigate | Use .get() for all dict keys (id, name, ble_address) with safe defaults; missing keys produce empty BtDeviceInfo, not crash |
| T-07-03-02 | Information Disclosure | BtDeviceInfo on _ui_queue | accept | Queue is internal process memory; no network exposure |
| T-07-03-03 | Tampering | hid.enumerate() path bytes | mitigate | Decode with errors="replace"; product_string empty-check before use |
</threat_model>

<verification>
Full test suite after this plan:
  python -m pytest tests/ -x -q

Specific BT service tests:
  python -m pytest tests/test_service.py::TestBtDevices -x -q

Import check:
  python -c "from monitor.state import BtDeviceInfo, BtScanResultEvent; from monitor.service import MonitorService; print('OK')"

Config backward compatibility:
  python -c "from ui.settings_manager import load_config; c = load_config(); assert isinstance(c.get('monitored_devices'), list); print('OK')"
</verification>

<success_criteria>
- BtDeviceInfo(bt_id, name, battery, ble_address, status) importable from monitor.state
- BtScanResultEvent(devices) importable from monitor.state
- _DEFAULTS in settings_manager.py contains monitored_devices: []
- MonitorService has scan_bt_devices() public method (thread-safe, returns Future)
- MonitorService has _bt_devices dict and _run_bt_scan() coroutine
- _run_bt_scan() calls both winrt_enumerate_bt() and hid.enumerate(); merges results into BtScanResultEvent.devices with type='hid' for HID entries
- discover() loads persisted BT devices from config["monitored_devices"]
- poll_once() refreshes BT battery via resolve_battery() passing battery=bt_info.battery (WinRT cached value) — not hardcoded None
- 5 new tests in TestBtDevices pass; full suite green
</success_criteria>

<output>
Create `.planning/phases/07-bluetooth-device-discovery/07-03-SUMMARY.md` when done.
</output>
