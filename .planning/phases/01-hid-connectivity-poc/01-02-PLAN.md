---
id: "01-02"
name: "Threading Architecture Stub"
phase: 1
plan: 2
type: execute
wave: 2
depends_on:
  - "01-01"
autonomous: true
files_modified:
  - src/threading_stub.py
requirements_addressed: []
must_haves:
  truths:
    - "asyncio background thread starts, runs a coroutine, posts one message to queue.Queue, and stops cleanly"
    - "QTimer on the Qt main thread drains queue.Queue every 500ms and updates a QLabel"
    - "QLabel text changes from 'Waiting...' to 'Received: hello from asyncio' before the window closes"
    - "script exits 0 with no dangling threads (bg_thread.join(timeout=5.0) completes)"
    - "sys.coinit_flags = 0 is the second line of threading_stub.py (after import sys) with explanatory comment"
  artifacts:
    - path: "src/threading_stub.py"
      provides: "Reusable asyncio + queue.Queue + QTimer pattern template for Phase 3 MonitorService"
      contains: "run_coroutine_threadsafe"
  key_links:
    - from: "background_task coroutine"
      to: "queue.Queue.put()"
      via: "ui_queue.put() call inside the coroutine running on the asyncio thread"
      pattern: "ui_queue\\.put"
    - from: "QTimer.timeout signal"
      to: "drain_queue() slot"
      via: "timer.timeout.connect()"
      pattern: "timer\\.timeout\\.connect"
    - from: "main() shutdown"
      to: "bg_thread.join()"
      via: "loop.call_soon_threadsafe(loop.stop) then bg_thread.join(timeout=5.0)"
      pattern: "call_soon_threadsafe.*loop\\.stop"
user_setup: []
---

<objective>
Create src/threading_stub.py: a minimal, self-contained demonstration of the asyncio-background-thread + queue.Queue + Qt-main-thread pattern that will be the template for MonitorService in Phase 3.

Purpose: Phase 3 cannot begin until the threading model is verified end-to-end. This stub proves all four cross-thread mechanisms work correctly in isolation — before any HID I/O is added to the mix.

Output:
- src/threading_stub.py: standalone PySide6 script with asyncio background thread, queue.Queue producer, QTimer consumer, and clean shutdown
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@f:\Cursor\batteryChecker\.planning\ROADMAP.md
@f:\Cursor\batteryChecker\.planning\STATE.md
@f:\Cursor\batteryChecker\CLAUDE.md
@f:\Cursor\batteryChecker\.planning\phases\01-hid-connectivity-poc\01-RESEARCH.md
@f:\Cursor\batteryChecker\.planning\phases\01-hid-connectivity-poc\01-01-SUMMARY.md
</context>

<interfaces>
<!-- Cross-thread contracts this plan must implement correctly. -->
<!-- Source: RESEARCH.md Pattern 4 and Python stdlib docs. -->

asyncio cross-thread scheduling:
  asyncio.run_coroutine_threadsafe(coro, loop) -> concurrent.futures.Future
    # Submits coro to loop from any thread. loop must be running (run_forever).
    # Thread-safe. Returns immediately; coro runs on the asyncio thread.

  loop.call_soon_threadsafe(callback, *args)
    # Schedules callback on the loop's thread. Thread-safe.
    # Use loop.call_soon_threadsafe(loop.stop) for clean shutdown from outside.

queue.Queue cross-thread messaging:
  ui_queue: queue.Queue = queue.Queue()
  ui_queue.put(item)       # thread-safe; called from asyncio thread
  ui_queue.get_nowait()    # raises queue.Empty when empty; called from Qt main thread

QTimer drain pattern:
  timer = QTimer()
  timer.timeout.connect(drain_slot)  # drain_slot called on Qt main thread every N ms
  timer.start(500)                    # 500ms interval

Background thread start:
  bg_loop = asyncio.new_event_loop()
  bg_thread = threading.Thread(target=run_background, args=(bg_loop,), daemon=True)
  bg_thread.start()
  # run_background calls: asyncio.set_event_loop(loop); loop.run_forever()
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create threading_stub.py with full asyncio + queue.Queue + QTimer pattern</name>
  <files>src/threading_stub.py</files>

  <read_first>
  - f:\Cursor\batteryChecker\.planning\phases\01-hid-connectivity-poc\01-RESEARCH.md — Pattern 3 (coinit_flags), Pattern 4 (full threading example with code), Anti-Patterns section (asyncio.Queue warning, asyncio.run() warning)
  - f:\Cursor\batteryChecker\CLAUDE.md — threading model invariants, queue.Queue requirement
  </read_first>

  <action>
  Create src\threading_stub.py as a standalone PySide6 script. This file is NOT a module; it is a self-contained PoC that can be run directly with: python src\threading_stub.py

  REQUIRED STRUCTURE — implement in this exact order:

  BLOCK 1 — COM guard (identical pattern as __main__.py):
    Line 1: import sys
    Line 2: sys.coinit_flags = 0  # MUST be first — same reason as in __main__.py. This file may be run directly as __main__. pythoncom reads this flag once on first COM init; PySide6 imports must not precede it.
    Line 3: (blank)

  BLOCK 2 — All other imports:
    import asyncio
    import queue
    import threading
    from PySide6.QtWidgets import QApplication, QLabel, QVBoxLayout, QWidget
    from PySide6.QtCore import QTimer

  BLOCK 3 — Module-level shared state:
    ui_queue: queue.Queue = queue.Queue()
    # This is the ONLY communication channel between threads.
    # asyncio background thread calls ui_queue.put(); Qt main thread calls ui_queue.get_nowait().
    # Do NOT use asyncio.Queue here — it is not thread-safe across threads (only safe within one event loop).

  BLOCK 4 — Background asyncio worker (two functions):

    async def background_task(loop: asyncio.AbstractEventLoop) -> None:
      """Simulates async I/O work on the background thread. Posts one message and stops the loop."""
      await asyncio.sleep(0.5)          # simulate async I/O latency
      ui_queue.put("hello from asyncio")  # thread-safe: queue.Queue.put() is always safe
      loop.call_soon_threadsafe(loop.stop)  # signal the loop to stop after this coroutine

    def run_background(loop: asyncio.AbstractEventLoop) -> None:
      """Entry point for the daemon background thread. Runs the asyncio event loop."""
      asyncio.set_event_loop(loop)
      loop.run_forever()
      # run_forever() blocks until loop.stop() is called.
      # After stop(), any pending callbacks complete, then this function returns.

  BLOCK 5 — Qt main thread drain slot:

    def drain_queue(label: QLabel) -> None:
      """Called by QTimer every 500ms on the Qt main thread. Drains all pending messages."""
      try:
        while True:
          msg = ui_queue.get_nowait()  # raises queue.Empty when nothing left
          label.setText(f"Received: {msg}")
          print(f"[main thread] drain_queue: {msg}")
      except queue.Empty:
        pass  # normal: nothing to drain this tick

  BLOCK 6 — main() function:

    def main() -> None:
      app = QApplication([])

      # Create a minimal window to show the received message
      window = QWidget()
      window.setWindowTitle("Threading Stub PoC")
      layout = QVBoxLayout(window)
      label = QLabel("Waiting...")
      layout.addWidget(label)
      window.show()

      # Start asyncio background thread (daemon=True so it does not block process exit)
      bg_loop = asyncio.new_event_loop()
      bg_thread = threading.Thread(target=run_background, args=(bg_loop,), daemon=True)
      bg_thread.start()

      # Submit the background coroutine from the main thread (thread-safe)
      # run_coroutine_threadsafe returns a Future; we discard it here (fire and forget for PoC)
      asyncio.run_coroutine_threadsafe(background_task(bg_loop), bg_loop)

      # QTimer drains the queue on the main thread every 500ms
      timer = QTimer()
      timer.timeout.connect(lambda: drain_queue(label))
      timer.start(500)

      # Auto-close the window 2 seconds after the message arrives so the PoC exits without user action
      # This is achieved by connecting a second QTimer that fires once after 2500ms
      close_timer = QTimer()
      close_timer.setSingleShot(True)
      close_timer.timeout.connect(app.quit)
      close_timer.start(2500)  # 500ms for message + 2000ms for human to observe

      app.exec()

      # Clean shutdown: stop the asyncio loop and wait for the thread to exit
      # This is the canonical shutdown sequence — do not skip join()
      bg_loop.call_soon_threadsafe(bg_loop.stop)
      bg_thread.join(timeout=5.0)
      if bg_thread.is_alive():
        print("[main] WARNING: background thread did not stop within 5 seconds")
      else:
        print("[main] background thread joined cleanly")

  BLOCK 7 — Entry guard:
    if __name__ == "__main__":
      main()

  IMPORTANT — what NOT to include:
  - No hid imports (this file has no hardware dependency)
  - No asyncio.run() calls — use new_event_loop() + run_forever() + run_coroutine_threadsafe() only
  - No asyncio.Queue — use queue.Queue (stdlib) for all cross-thread messaging
  - No qasync or event loop merging
  </action>

  <verify>
    <automated>python -c "src=open('src/threading_stub.py').read(); lines=src.splitlines(); assert lines[0].strip()=='import sys', f'Line 1: {lines[0]}'; assert lines[1].strip().startswith('sys.coinit_flags = 0'), f'Line 2: {lines[1]}'; print('coinit_flags position: PASS')"</automated>
    <automated>python -c "src=open('src/threading_stub.py').read(); assert 'run_coroutine_threadsafe' in src, 'Missing run_coroutine_threadsafe'; assert 'call_soon_threadsafe' in src, 'Missing call_soon_threadsafe'; assert 'bg_thread.join' in src, 'Missing thread join'; assert 'queue.Queue' in src, 'Missing queue.Queue'; assert 'asyncio.Queue' not in src, 'asyncio.Queue must not be used'; assert 'asyncio.run(' not in src, 'asyncio.run() must not be used'; print('threading pattern: PASS')"</automated>
    <automated>python src/threading_stub.py</automated>
  </verify>

  <done>
  - src/threading_stub.py exists
  - coinit_flags on line 2 with explanatory comment
  - run_coroutine_threadsafe, call_soon_threadsafe, bg_thread.join() all present
  - No asyncio.Queue, no asyncio.run()
  - python src/threading_stub.py: window opens, label changes from "Waiting..." to "Received: hello from asyncio", script exits 0
  - stdout includes "[main thread] drain_queue: hello from asyncio" and "[main] background thread joined cleanly"
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  src/threading_stub.py is a self-contained PySide6 script demonstrating the full asyncio-background-thread + queue.Queue + Qt-main-thread + clean-shutdown pattern. It posts one message from an asyncio coroutine to the Qt main thread via queue.Queue, displays it in a QLabel via a QTimer drain, then shuts down cleanly.
  </what-built>
  <how-to-verify>
  1. Run: python src/threading_stub.py
     Expected sequence of events:
       a. A window titled "Threading Stub PoC" opens showing "Waiting..."
       b. Within ~500ms the label changes to "Received: hello from asyncio"
       c. After ~2500ms the window closes automatically
       d. Terminal prints: "[main thread] drain_queue: hello from asyncio"
       e. Terminal prints: "[main] background thread joined cleanly"
       f. Process exits with code 0 (no traceback, no hanging)

  2. Verify exit code: echo $?  (bash) or echo $LASTEXITCODE  (PowerShell)
     Expected: 0

  3. Verify no asyncio.Queue usage in the source:
     python -c "src=open('src/threading_stub.py').read(); assert 'asyncio.Queue' not in src; print('queue.Queue only: PASS')"

  4. Verify coinit_flags position:
     python -c "lines=open('src/threading_stub.py').readlines(); assert 'coinit_flags' in lines[1]; print('coinit_flags line 2: PASS')"
  </how-to-verify>
  <resume-signal>Type "approved" to confirm Phase 1 threading architecture is validated, or describe any issues (e.g., window did not update, thread did not join, exit code non-zero).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| asyncio background thread -> queue.Queue -> Qt main thread | All cross-thread data flows through queue.Queue; no shared mutable state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | bg_thread.join(timeout=5.0) | mitigate | 5-second timeout prevents indefinite hang if loop.stop() fails to propagate; is_alive() check prints warning |
| T-02-02 | Tampering | queue.Queue message content | accept | Phase 1 stub posts a hardcoded string literal; no external input; no parsing; zero attack surface |
| T-02-SC | Tampering | PySide6 already installed from Plan 01 | accept | Package verified in Plan 01; no new installs in this plan |
</threat_model>

<verification>
Run each check after execution to confirm Phase 1 Plan 02 success criteria:

1. Static check — coinit_flags position in threading_stub.py:
   python -c "lines=open('src/threading_stub.py').readlines(); assert lines[0].strip()=='import sys'; assert 'coinit_flags' in lines[1]; print('SC-3 (threading_stub): PASS')"

2. Pattern compliance checks:
   python -c "src=open('src/threading_stub.py').read(); checks={'run_coroutine_threadsafe':True,'call_soon_threadsafe':True,'queue.Queue':True,'bg_thread.join':True,'asyncio.Queue':False,'asyncio.run(':False}; [(__import__('sys').exit(f'FAIL: {k} presence={v} expected={expected}') if (k in src)!=expected else None) for k,expected in checks.items()]; print('threading pattern compliance: PASS')"

3. End-to-end smoke test:
   python src/threading_stub.py
   Expected: exits 0, stdout contains "hello from asyncio" and "background thread joined cleanly"
</verification>

<success_criteria>
- src/threading_stub.py exists and runs without error
- sys.coinit_flags = 0 is line 2 (after import sys) with explanatory comment
- Window shows "Received: hello from asyncio" (confirming queue message crossed thread boundary)
- Script exits 0 with "[main] background thread joined cleanly" in stdout (confirming clean shutdown)
- No asyncio.Queue, no asyncio.run() in the file
- This file is ready to serve as the template for Phase 3 MonitorService
</success_criteria>

<output>
When all tasks and verifications pass, create:
  f:\Cursor\batteryChecker\.planning\phases\01-hid-connectivity-poc\01-02-SUMMARY.md

The summary must record:
- Observed stdout output from the smoke test run (copy the key lines)
- Confirmation that bg_thread.join() completed within timeout
- Any deviations from the plan and why
- Note: "This file is the Phase 3 MonitorService template — preserve the queue.Queue + QTimer pattern verbatim"
</output>
