---
phase: 05-steelseries-hid-backend
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/monitor/state.py
  - src/monitor/service.py
  - tests/test_service.py
autonomous: true
requirements:
  - HID-02

must_haves:
  truths:
    - "KNOWN_DEVICES in state.py contains (0x1038, 0x1852): 'Aerox 5 Wireless'"
    - "DEVICE_PROBES in state.py maps (0x046D, 0x0ABA) to battery_probe_chain and (0x1038, 0x1852) to ss_battery_probe"
    - "poll_once() dispatches via DEVICE_PROBES[(vid,pid)] instead of hardcoded battery_probe_chain"
    - "SteelSeries entries in self._open store info dict (not open handle); poll_once() opens fresh, probes, closes per poll"
    - "voltage_mv==0 guard in poll_once() uses result.percent directly without updating _voltage_history"
    - "discover() calls find_dongle() for SteelSeries in addition to find_receiver() for Logitech"
    - "Existing test_service.py tests updated to patch DEVICE_PROBES instead of battery_probe_chain"
    - "All tests pass: pytest tests/test_service.py -x"
  artifacts:
    - path: "src/monitor/state.py"
      provides: "KNOWN_DEVICES (updated) + DEVICE_PROBES (new)"
      contains: "DEVICE_PROBES"
    - path: "src/monitor/service.py"
      provides: "Updated discover() + poll_once() with DEVICE_PROBES dispatch and voltage smoothing guard"
    - path: "tests/test_service.py"
      provides: "Updated + extended tests for DEVICE_PROBES dispatch, smoothing guard, SS offline"
  key_links:
    - from: "src/monitor/service.py"
      to: "src/monitor/state.py"
      via: "from monitor.state import KNOWN_DEVICES, DEVICE_PROBES, DeviceState, DeviceStatus"
      pattern: "DEVICE_PROBES"
    - from: "src/monitor/service.py"
      to: "src/steelseries/driver.py"
      via: "DEVICE_PROBES dispatch (indirect — driver imported in state.py)"
      pattern: "DEVICE_PROBES\\.get"
    - from: "src/monitor/service.py"
      to: "src/steelseries/driver.py"
      via: "from steelseries.driver import find_dongle, open_dongle"
      pattern: "find_dongle|open_dongle"
---

<objective>
Wire the SteelSeries backend into MonitorService. Three changes:
1. state.py: add SteelSeries to KNOWN_DEVICES and introduce DEVICE_PROBES dispatch dict (per D-02)
2. service.py: update discover() to enumerate SteelSeries dongles; update poll_once() to dispatch via DEVICE_PROBES with per-poll open/close for SteelSeries; add voltage_mv==0 smoothing guard (per D-03)
3. tests/test_service.py: migrate existing patches from battery_probe_chain to DEVICE_PROBES; add new tests for SS dispatch, smoothing guard, SS unplug offline path

Purpose: Makes MonitorService device-agnostic. Adding a new device in the future requires only a KNOWN_DEVICES + DEVICE_PROBES entry, with no changes to poll_once() or discover() logic.

Output: Updated src/monitor/state.py, src/monitor/service.py, tests/test_service.py
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-steelseries-hid-backend/05-RESEARCH.md
@.planning/phases/05-steelseries-hid-backend/05-PATTERNS.md
@.planning/phases/05-steelseries-hid-backend/05-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts. Extracted from codebase. -->

From src/monitor/state.py (current — to be modified):
```python
KNOWN_DEVICES: dict[tuple[int, int], str] = {
    (0x046D, 0x0ABA): "G Pro X Wireless",
}
```

From src/monitor/service.py (current poll_once — lines 208-240 to be updated):
```python
async def poll_once(self) -> None:
    for key, handle in list(self._open.items()):
        vid, pid, dev_idx = key
        result = battery_probe_chain(handle, dev_idx)   # <-- line to replace
        if result is None or result.percent == 0:
            ...
        else:
            hist = self._voltage_history.setdefault(key, deque(maxlen=_VOLTAGE_WINDOW))
            hist.append(result.voltage_mv)              # <-- smoothing (needs guard)
            smoothed_percent = voltage_to_percent(round(sum(hist) / len(hist)))
```

From src/monitor/service.py (current discover — lines 134-190 to be updated):
```python
async def discover(self) -> None:
    interfaces = find_receiver(verbose=False)   # <-- only Logitech; must also enumerate SS
    found_keys: set = set()
    for info in interfaces:
        vid = info["vendor_id"]
        pid = info["product_id"]
        if (vid, pid) in KNOWN_DEVICES:
            found_keys.add((vid, pid, DEVICE_IDX))
    # mark disappeared OFFLINE, open new handles...
    for info in interfaces:
        ...
        key = (vid, pid, DEVICE_IDX)   # <-- DEVICE_IDX is Logitech-specific; SS uses SS_DEVICE_IDX
        handle = open_receiver(info)   # <-- must use open_dongle for SS
```

From src/steelseries/driver.py (created in 05-01):
```python
SS_VID = 0x1038
SS_AEROX5_PID = 0x1852
SS_VENDOR_INTERFACE = 3
SS_DEVICE_IDX = 0x00

def find_dongle(vid: int = SS_VID, verbose: bool = False) -> list[dict]: ...
def open_dongle(info: dict): ...
def ss_battery_probe(device, dev_idx: int) -> "BatteryResult | None": ...
```

From tests/test_service.py (existing patches that MUST be migrated):
```python
# Lines 168-171 — existing battery_probe_chain patches that will break after state.py change:
mocker.patch("monitor.service.battery_probe_chain", return_value=BatteryResult(...))
# These appear in: test_online_battery_result_upserts_and_queues,
#   test_charging_result_yields_charging_status,
#   test_none_result_marks_offline_keeps_handle,
#   test_none_result_does_not_queue_if_not_in_registry
# ALL must be migrated to patch DEVICE_PROBES.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update src/monitor/state.py (KNOWN_DEVICES + DEVICE_PROBES) and src/monitor/service.py (discover + poll_once)</name>
  <files>src/monitor/state.py, src/monitor/service.py</files>
  <read_first>
    - src/monitor/state.py (full file — modify KNOWN_DEVICES, add DEVICE_PROBES)
    - src/monitor/service.py (full file — modify imports, discover(), poll_once())
    - src/steelseries/driver.py (exports consumed here: SS_DEVICE_IDX, find_dongle, open_dongle, ss_battery_probe)
    - src/hidpp/receiver.py (DEVICE_IDX constant used in existing discover() logic)
    - .planning/phases/05-steelseries-hid-backend/05-PATTERNS.md (Pattern 3: DEVICE_PROBES; Pattern 4: poll_once dispatch; Pattern 5: smoothing guard; handle management Strategy A/B discussion)
    - .planning/phases/05-steelseries-hid-backend/05-RESEARCH.md (Open Questions section on handle management; Pitfall 1 and Pitfall 6)
  </read_first>
  <action>
    CHANGE 1 — src/monitor/state.py:

    Add imports at top of file (after existing imports): "from hidpp.features import battery_probe_chain" and "from steelseries.driver import ss_battery_probe". These imports are safe — neither hidpp.features nor steelseries.driver imports from monitor.

    Extend KNOWN_DEVICES to add "(0x1038, 0x1852): 'Aerox 5 Wireless'" as the second entry.

    Immediately after KNOWN_DEVICES, add:
    DEVICE_PROBES: dict[tuple[int, int], callable] = {
        (0x046D, 0x0ABA): battery_probe_chain,
        (0x1038, 0x1852): ss_battery_probe,
    }
    Include docstring comment: "Probe function registry: (vid, pid) → (device_or_info, dev_idx) → BatteryResult | None. For Logitech, the first argument is an open hid.device handle. For SteelSeries, the first argument is the info dict (open_dongle is called per-poll inside poll_once)."

    CHANGE 2 — src/monitor/service.py imports section:

    Remove "battery_probe_chain" from the "from hidpp.features import ..." line (it is now in DEVICE_PROBES). Keep "voltage_to_percent".
    Add to the "from monitor.state import ..." line: add "DEVICE_PROBES" to the existing import.
    Add new import line after the hidpp imports: "from steelseries.driver import SS_DEVICE_IDX, find_dongle, open_dongle".

    CHANGE 3 — src/monitor/service.py discover():

    Redesign discover() to enumerate both Logitech and SteelSeries interfaces. The cleanest approach: build a unified list of (info, device_idx, open_fn) tuples, then process them with the same discovered/mark-offline logic.

    Specifically: call find_receiver(verbose=False) for Logitech interfaces (dev_idx=DEVICE_IDX, open_fn=open_receiver). Call find_dongle(verbose=False) for SteelSeries interfaces (dev_idx=SS_DEVICE_IDX, open_fn=None — SteelSeries handles are NOT opened persistently).

    In the found_keys loop: for Logitech infos, found_keys.add((vid, pid, DEVICE_IDX)); for SteelSeries infos, found_keys.add((vid, pid, SS_DEVICE_IDX)).

    In the open-new-handles loop: for Logitech, store open_receiver(info) in self._open[key] as before. For SteelSeries (identified by vid==SS_VID or by checking (vid,pid) against a set of SS PIDs), store the raw info dict in self._open[key] — do NOT call open_dongle here. Initial DeviceState is pushed with status=ONLINE as before.

    Pattern for identifying device backend: check "(vid, pid)" — if (vid, pid) maps to ss_battery_probe in DEVICE_PROBES (i.e., DEVICE_PROBES.get((vid,pid)) is ss_battery_probe), store info dict. Otherwise store open_receiver(info).

    In the mark-disappeared-offline loop: for SteelSeries keys, self._open[key] is an info dict (not an open handle), so the try/handle.close() block must guard against calling close() on a dict. Simple approach: try/except AttributeError around handle.close().

    CHANGE 4 — src/monitor/service.py poll_once():

    Replace hardcoded "result = battery_probe_chain(handle, dev_idx)" with:
        probe_fn = DEVICE_PROBES.get((vid, pid))
        if probe_fn is None:
            continue
        if probe_fn is ss_battery_probe:
            # SteelSeries: open fresh handle per poll (dongle responds once per open)
            try:
                fresh_handle = open_dongle(handle)  # handle is the info dict
                result = probe_fn(fresh_handle, dev_idx)
            except OSError:
                result = None
            finally:
                try:
                    fresh_handle.close()
                except Exception:
                    pass
        else:
            result = probe_fn(handle, dev_idx)

    Note: "handle" in the SteelSeries branch is the info dict stored by discover(). open_dongle(info_dict) opens a fresh connection.

    CHANGE 5 — src/monitor/service.py poll_once() smoothing guard (D-03):

    Replace the unconditional voltage smoothing block:
        hist = self._voltage_history.setdefault(key, deque(maxlen=_VOLTAGE_WINDOW))
        hist.append(result.voltage_mv)
        smoothed_percent = voltage_to_percent(round(sum(hist) / len(hist)))

    With the guarded version:
        if result.voltage_mv != 0:
            hist = self._voltage_history.setdefault(key, deque(maxlen=_VOLTAGE_WINDOW))
            hist.append(result.voltage_mv)
            smoothed_percent = voltage_to_percent(round(sum(hist) / len(hist)))
        else:
            # SteelSeries reports voltage_mv=0; use percent directly, no smoothing
            smoothed_percent = result.percent

    SURGICAL CHANGE NOTE: Only modify the five specific sections above. Do not touch docstrings, the stop() method, the rescan() method, the SIGINT timer, or any other parts of service.py.
  </action>
  <verify>
    <automated>cd F:\Cursor\batteryChecker && .venv-1\Scripts\python.exe -c "from monitor.state import KNOWN_DEVICES, DEVICE_PROBES; assert (0x1038, 0x1852) in KNOWN_DEVICES; assert (0x1038, 0x1852) in DEVICE_PROBES; print('state.py OK')" 2>&1</automated>
  </verify>
  <done>
    - state.py: KNOWN_DEVICES has SteelSeries entry; DEVICE_PROBES dict defined with both (0x046D, 0x0ABA) and (0x1038, 0x1852) keys
    - service.py: imports updated (DEVICE_PROBES imported, battery_probe_chain removed from direct import, find_dongle/open_dongle/SS_DEVICE_IDX added)
    - service.py discover(): enumerates both find_receiver() and find_dongle(); stores info dict for SS in self._open; works with existing mark-offline logic
    - service.py poll_once(): dispatches via DEVICE_PROBES; SteelSeries path opens fresh handle, probes, closes; voltage_mv==0 guard present
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update tests/test_service.py — migrate battery_probe_chain patches to DEVICE_PROBES and add new SS tests</name>
  <files>tests/test_service.py</files>
  <behavior>
    Existing tests that patch "monitor.service.battery_probe_chain":
    - test_online_battery_result_upserts_and_queues: patch → mocker.patch.dict("monitor.service.DEVICE_PROBES", {(GPRO_VID, GPRO_PID): lambda h, i: BatteryResult(75, 3990, False, "0x06/0x0D")})
    - test_charging_result_yields_charging_status: same migration, return BatteryResult(60, 3894, True, "0x06/0x0D")
    - test_none_result_marks_offline_keeps_handle: patch → lambda returning None
    - test_none_result_does_not_queue_if_not_in_registry: patch → lambda returning None

    New tests to add:

    test_dispatches_via_device_probes (in TestPollOnce): confirm poll_once calls the probe_fn from DEVICE_PROBES, not battery_probe_chain directly. Use a MagicMock probe function. Assert it was called with (handle, DEVICE_IDX).

    test_zero_voltage_skips_smoothing (in TestPollOnce): inject a SteelSeries key into service._open as an info dict. Patch DEVICE_PROBES to include SS entry returning BatteryResult(20, 0, False, "0xD2"). Also patch open_dongle to return a MagicMock and patch ss_battery_probe detection (use probe_fn is ss_battery_probe check in service — alternatively patch DEVICE_PROBES with a sentinel lambda that returns voltage_mv=0). Run asyncio.run(service.poll_once()). Assert registry.get(ss_key).percent == 20. Assert ss_key NOT in service._voltage_history (smoothing not applied).

    test_ss_dongle_unplug_marks_offline (in TestDiscover): pre-populate service._open with SS key mapped to an info dict (simulating SS already discovered). Patch find_receiver to return empty (Logitech gone). Patch find_dongle to return empty (SS gone). Run asyncio.run(service.discover()). Assert SS key is removed from service._open. Assert registry.get(ss_key).status == DeviceStatus.OFFLINE.

    test_ss_discover_stores_info_dict (in TestDiscover): Patch find_dongle to return one SS interface dict. Patch find_receiver to return empty. Run asyncio.run(service.discover()). Assert (SS_VID, SS_PID, SS_DEVICE_IDX) in service._open. Assert service._open[(SS_VID, SS_PID, SS_DEVICE_IDX)] is a dict (not a hid.device handle) — i.e., isinstance(..., dict).

    SS_VID = 0x1038, SS_PID = 0x1852. Import SS_DEVICE_IDX from steelseries.driver at the top of the test file.

    For the open_dongle mock in test_zero_voltage_skips_smoothing: since poll_once() branches on "probe_fn is ss_battery_probe", the DEVICE_PROBES patch must map SS key to the actual ss_battery_probe function (not a lambda). Patch open_dongle at "monitor.service.open_dongle" to return a fresh MagicMock. The actual ss_battery_probe call on that mock will return [] reads → None. To force a result of BatteryResult(20, 0, False, "0xD2"), patch "steelseries.driver.ss_battery_probe" OR restructure: since the test needs to verify smoothing guard logic, it's acceptable to patch ss_battery_probe at "steelseries.driver" namespace and import it. Simplest approach: patch "monitor.service.open_dongle" to return a MagicMock; patch "monitor.service.DEVICE_PROBES" with actual ss_battery_probe for SS but also patch "steelseries.driver.ss_battery_probe" to return BatteryResult(20, 0, False, "0xD2").
  </behavior>
  <read_first>
    - tests/test_service.py (full file — all existing tests must be understood before migrating patches)
    - src/monitor/service.py (updated version from Task 1 — understand the DEVICE_PROBES dispatch logic and SS branch)
    - src/monitor/state.py (updated DEVICE_PROBES to know what's importable)
    - src/steelseries/driver.py (SS_DEVICE_IDX constant, ss_battery_probe signature)
    - .planning/phases/05-steelseries-hid-backend/05-PATTERNS.md (test_service.py section — mocker.patch.dict patterns)
  </read_first>
  <action>
    Edit tests/test_service.py (do not rewrite from scratch — surgical edits only):

    1. Add to imports at top: "from steelseries.driver import SS_DEVICE_IDX" and "from steelseries.driver import ss_battery_probe as _ss_battery_probe".

    2. Add SS constants below GPRO constants:
       SS_VID = 0x1038
       SS_PID = 0x1852
       SS_KEY = (SS_VID, SS_PID, SS_DEVICE_IDX)
       SS_INTERFACE = {"vendor_id": SS_VID, "product_id": SS_PID, "interface_number": 3, "usage_page": 0xFFC0, "path": b"/dev/hid3"}

    3. Migrate the four existing battery_probe_chain patches in TestPollOnce tests to mocker.patch.dict("monitor.service.DEVICE_PROBES", {(GPRO_VID, GPRO_PID): <lambda>}). Keep exact same test names and assertions — only change the mocker.patch line.

    4. Add test_dispatches_via_device_probes to TestPollOnce: Create a MagicMock probe_fn returning BatteryResult(75, 3990, False, "x"). Patch DEVICE_PROBES with {(GPRO_VID, GPRO_PID): probe_fn}. Also need to prevent SS branch from triggering — since service._open only has GPRO_KEY in the helper, no SS key present. Run poll_once(). Assert probe_fn.called and probe_fn.call_args[0] == (service._open[GPRO_KEY], DEVICE_IDX).

    5. Add test_zero_voltage_skips_smoothing to TestPollOnce: Setup service using _setup_with_open_device. Add SS key to service._open with a dummy info dict {"vendor_id": SS_VID, "product_id": SS_PID, "interface_number": 3, "path": b"/dev/hid3"}. Pre-populate registry with SS DeviceState (ONLINE, percent=None). Patch "monitor.service.open_dongle" to return a MagicMock (fresh_handle). Patch "steelseries.driver.ss_battery_probe" to return BatteryResult(20, 0, False, "0xD2"). Patch DEVICE_PROBES with {(GPRO_VID, GPRO_PID): lambda h,i: BatteryResult(75, 3990, False, "0x06/0x0D"), (SS_VID, SS_PID): _ss_battery_probe}. Run asyncio.run(service.poll_once()). Assert registry.get(SS_KEY).percent == 20. Assert SS_KEY not in service._voltage_history.

    6. Add test_ss_dongle_unplug_marks_offline to TestDiscover: Create service. Pre-populate service._open[SS_KEY] = SS_INTERFACE (info dict). Pre-populate registry with SS DeviceState ONLINE. Patch find_receiver to return [] (Logitech gone). Patch find_dongle to return [] (SS gone). Run asyncio.run(service.discover()). Assert SS_KEY not in service._open. Assert registry.get(SS_KEY).status == DeviceStatus.OFFLINE.

    7. Add test_ss_discover_stores_info_dict to TestDiscover: Create service. Patch find_receiver to return []. Patch find_dongle to return [SS_INTERFACE]. Run asyncio.run(service.discover()). Assert SS_KEY in service._open. Assert isinstance(service._open[SS_KEY], dict).

    IMPORTANT: After migrations, run full test suite mentally — the TestSafetyInvariants tests must still pass (they check service.py source for PySide6 and hid.open; our changes don't add either).
  </action>
  <verify>
    <automated>cd F:\Cursor\batteryChecker && .venv-1\Scripts\python.exe -m pytest tests/test_service.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All existing test_service.py tests pass (battery_probe_chain patches migrated to DEVICE_PROBES)
    - test_dispatches_via_device_probes passes
    - test_zero_voltage_skips_smoothing passes (SS_KEY not in _voltage_history after poll)
    - test_ss_dongle_unplug_marks_offline passes
    - test_ss_discover_stores_info_dict passes
    - Full suite: pytest tests/ -x passes with no regressions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DEVICE_PROBES dispatch | Probe functions are module-level callables; no untrusted code path |
| SteelSeries info dict in self._open | Dict from hid.enumerate; treated as read-only; path byte string used for open_path only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | poll_once() SteelSeries open/close per poll | accept | OSError on open_dongle is caught; result=None → mark OFFLINE via existing path |
| T-05-04 | Denial of Service | discover() storing info dict for SS (no open handle) | accept | close() protected by try/except AttributeError; no crash if dict stored instead of handle |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced in Phase 5 |
</threat_model>

<verification>
pytest tests/test_service.py -v — all tests green including 4 migrated + 4 new
pytest tests/ -x — full suite (140+ tests) green
python -c "from monitor.state import DEVICE_PROBES; from hidpp.features import battery_probe_chain; from steelseries.driver import ss_battery_probe; assert DEVICE_PROBES[(0x046D, 0x0ABA)] is battery_probe_chain; assert DEVICE_PROBES[(0x1038, 0x1852)] is ss_battery_probe; print('DEVICE_PROBES OK')"
python -c "from monitor.service import MonitorService; import queue; from monitor.registry import DeviceRegistry; s = MonitorService(queue.Queue(), DeviceRegistry()); print('service import OK')"
</verification>

<success_criteria>
- state.py: KNOWN_DEVICES["Aerox 5 Wireless"] entry present; DEVICE_PROBES with both Logitech and SteelSeries keys
- service.py: battery_probe_chain no longer directly imported or hardcoded in poll_once()
- service.py: discover() calls both find_receiver() and find_dongle(); SteelSeries info dict stored (not open handle)
- service.py: poll_once() dispatches via DEVICE_PROBES; SteelSeries branch opens fresh handle via open_dongle(info), probes, closes
- service.py: voltage_mv==0 guard present; _voltage_history not updated for SteelSeries keys
- tests: all 4 battery_probe_chain patches migrated to DEVICE_PROBES patch.dict
- tests: 4 new tests added covering SS dispatch, smoothing guard, SS unplug offline, SS info dict storage
- Full pytest suite passes with no regressions
</success_criteria>

<output>
Create .planning/phases/05-steelseries-hid-backend/05-02-SUMMARY.md when done
</output>
