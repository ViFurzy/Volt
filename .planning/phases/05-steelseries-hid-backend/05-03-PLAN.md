---
phase: 05-steelseries-hid-backend
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified: []
autonomous: false
requirements:
  - HID-02

must_haves:
  truths:
    - "SteelSeries Aerox 5 Wireless appears as a live device card in the main window"
    - "Battery percentage shown on SS card is a non-zero value in 5% steps (matches physical device charge level)"
    - "Charging status shown correctly (True if device is on USB, False if wireless)"
    - "Unplugging the SteelSeries dongle marks the device OFFLINE in the UI within the 500ms debounce window"
    - "Logitech G Pro X Wireless continues to work correctly alongside the SteelSeries device"
    - "App starts cleanly with both dongles plugged in (no OSError, no crash on first poll)"
  artifacts: []
  key_links: []
---

<objective>
Hardware integration test for the SteelSeries backend. Run the full application with both the SteelSeries Aerox 5 Wireless dongle and Logitech LIGHTSPEED dongle connected. Verify that the SteelSeries device appears as a live card in the UI, battery percentage is accurate, and unplug/replug cycles work through the same code path as Logitech.

Purpose: Phase 5 success gate. Automated unit tests in 05-01/05-02 verify mock-based logic. This plan verifies hardware behavior and confirms HID-02 is satisfied on real hardware.

Output: Hardware checkpoint verification — no code files modified.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-steelseries-hid-backend/05-RESEARCH.md
@.planning/phases/05-steelseries-hid-backend/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated test suite and validate imports</name>
  <files></files>
  <read_first>
    - .planning/phases/05-steelseries-hid-backend/05-02-SUMMARY.md (confirm what was implemented)
  </read_first>
  <action>
    Run the following validation sequence:

    1. Full test suite: pytest tests/ -v — must pass 100% with no regressions. Count total tests and confirm >= 140 (129 prior + new Phase 5 tests).

    2. Import smoke test:
       python -c "from steelseries.driver import find_dongle, open_dongle, ss_battery_probe, SS_VID, SS_AEROX5_PID, SS_VENDOR_INTERFACE, SS_DEVICE_IDX; from monitor.state import KNOWN_DEVICES, DEVICE_PROBES; assert (0x1038, 0x1852) in KNOWN_DEVICES; assert (0x1038, 0x1852) in DEVICE_PROBES; print('All imports OK')"

    3. DEVICE_PROBES sanity check:
       python -c "from monitor.state import DEVICE_PROBES; from hidpp.features import battery_probe_chain; from steelseries.driver import ss_battery_probe; assert DEVICE_PROBES.get((0x046D, 0x0ABA)) is battery_probe_chain; assert DEVICE_PROBES.get((0x1038, 0x1852)) is ss_battery_probe; print('DEVICE_PROBES dispatch OK')"

    4. No-hid-open check on driver.py:
       python -c "import ast, pathlib; tree=ast.parse(pathlib.Path('src/steelseries/driver.py').read_text()); calls=[n for n in ast.walk(tree) if isinstance(n, ast.Call) and isinstance(getattr(n,'func',None), ast.Attribute) and n.func.attr=='open' and isinstance(n.func.value, ast.Name) and n.func.value.id=='hid']; assert not calls, f'hid.open found at lines {[n.lineno for n in calls]}'; print('No hid.open in driver.py — OK')"

    If any automated check fails, diagnose and fix before proceeding to the hardware checkpoint.
  </action>
  <verify>
    <automated>cd F:\Cursor\batteryChecker && .venv-1\Scripts\python.exe -m pytest tests/ -v 2>&1 | tail -15</automated>
  </verify>
  <done>pytest tests/ passes 100%; all import and sanity checks print OK</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 5 implementation:
    - src/steelseries/driver.py: find_dongle() (interface_number=3 filter), open_dongle() (open_path only), ss_battery_probe() (3 warmup reads + 0xD2 command + 20-packet scan)
    - src/monitor/state.py: KNOWN_DEVICES + DEVICE_PROBES with SteelSeries entries
    - src/monitor/service.py: discover() enumerates both dongles; poll_once() dispatches via DEVICE_PROBES; per-poll open/close for SteelSeries; voltage_mv==0 smoothing guard
    - All unit tests updated and passing
  </what-built>
  <how-to-verify>
    **Prerequisites:**
    - SteelSeries Aerox 5 Wireless (PID 0x1852) dongle plugged into USB
    - Logitech G Pro X Wireless (PID 0x0ABA) dongle plugged into USB
    - Mouse is powered on and connected wirelessly to the SteelSeries dongle

    **Step 1 — Run the application from the src/ directory:**
    cd F:\Cursor\batteryChecker
    .venv-1\Scripts\python.exe -m src.__main__

    **Step 2 — Verify initial state (within 10 seconds of startup):**
    - Two device cards appear in the main window: "G Pro X Wireless" and "Aerox 5 Wireless"
    - Both cards show a non-null battery percentage
    - SteelSeries battery % is a multiple of 5 (e.g., 20%, 25%, 30% — the protocol reports in 5% steps)
    - No error dialogs, no crash, no "Access Denied" in stderr

    **Step 3 — Verify SteelSeries battery accuracy:**
    - Note the displayed battery percentage for Aerox 5 Wireless
    - Mentally verify it's plausible (check the device LED if available, or recall last known charge level)

    **Step 4 — Unplug the SteelSeries dongle:**
    - Within ~1 second, the Aerox 5 Wireless card should change to OFFLINE state
    - The G Pro X Wireless card should remain ONLINE and unaffected

    **Step 5 — Replug the SteelSeries dongle:**
    - Within ~1 second, the Aerox 5 Wireless card should return to ONLINE with the same battery %
    - Confirm the 500ms debounce prevents duplicate ONLINE/OFFLINE flicker

    **Step 6 — Unplug/replug the Logitech dongle:**
    - Confirm G Pro X Wireless follows the same ONLINE/OFFLINE/ONLINE cycle
    - Confirm SteelSeries card is unaffected by Logitech dongle events

    **Step 7 — Close the app cleanly:**
    - Close to tray, then quit from tray menu (or Ctrl+C in terminal)
    - No "Task was destroyed but it is pending!" or OSError in output
  </how-to-verify>
  <resume-signal>
    Type "verified" if all 7 steps pass.
    Type "issue: [description]" to report any failure (include which step failed and what was observed).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Physical USB HID → Python process | Hardware checkpoint — live device, real bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Denial of Service | Physical dongle unplug during active poll cycle | accept | OSError caught in poll_once() open_dongle path; result=None → OFFLINE via existing path |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
Hardware checkpoint: all 7 steps in the human-verify task pass.

Automated pre-gate (Task 1 must pass before hardware checkpoint):
- pytest tests/ -v: 100% pass
- Import smoke test: prints "All imports OK"
- DEVICE_PROBES sanity: prints "DEVICE_PROBES dispatch OK"
- No hid.open check: prints "No hid.open in driver.py — OK"
</verification>

<success_criteria>
HID-02 satisfied:
1. SteelSeries Aerox 5 Wireless appears as a live device card in the main window (per ROADMAP Phase 5 success criterion 3)
2. Battery % displayed is a non-zero value in 5% steps (per success criterion 2 — raw HID output report + parsed response)
3. Dongle unplug marks device OFFLINE via the HID-04 code path — no duplicate offline logic (per success criterion 4)
4. Logitech G Pro X Wireless continues working alongside SteelSeries — no regression
5. App starts and exits cleanly with both dongles connected
</success_criteria>

<output>
Create .planning/phases/05-steelseries-hid-backend/05-03-SUMMARY.md when done
</output>
