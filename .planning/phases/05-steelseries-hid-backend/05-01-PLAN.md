---
phase: 05-steelseries-hid-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/steelseries/__init__.py
  - src/steelseries/driver.py
  - tests/test_steelseries_driver.py
autonomous: true
requirements:
  - HID-02

must_haves:
  truths:
    - "find_dongle() returns only interface_number=3 interfaces for the SteelSeries VID"
    - "open_dongle() opens via open_path(info['path']), never hid.open(vid, pid)"
    - "ss_battery_probe() parses level_byte=5 into percent=20, charging=False, voltage_mv=0, feature_used='0xD2'"
    - "ss_battery_probe() returns None when no 0xD2 response arrives within 20 reads"
    - "ss_battery_probe() issues exactly 3 warmup reads before writing the battery command"
    - "All tests pass: pytest tests/test_steelseries_driver.py -x"
  artifacts:
    - path: "src/steelseries/__init__.py"
      provides: "package marker (empty)"
    - path: "src/steelseries/driver.py"
      provides: "find_dongle, open_dongle, ss_battery_probe, SS_VID, SS_AEROX5_PID, SS_VENDOR_INTERFACE, SS_DEVICE_IDX"
      exports:
        - find_dongle
        - open_dongle
        - ss_battery_probe
        - SS_VID
        - SS_AEROX5_PID
        - SS_VENDOR_INTERFACE
        - SS_DEVICE_IDX
    - path: "tests/test_steelseries_driver.py"
      provides: "unit tests for all driver functions"
  key_links:
    - from: "src/steelseries/driver.py"
      to: "src/hidpp/features.py"
      via: "from hidpp.features import BatteryResult"
      pattern: "from hidpp\\.features import BatteryResult"
---

<objective>
Create the src/steelseries/ package with the SteelSeries Aerox 5 Wireless HID driver. Implement find_dongle(), open_dongle(), and ss_battery_probe() following the same structural pattern as src/hidpp/receiver.py and src/hidpp/features.py. Write the complete unit test suite.

Purpose: Provide an isolated, testable protocol layer for the SteelSeries Aerox 5 Wireless. This module is consumed by Plan 05-02 which wires it into MonitorService via DEVICE_PROBES.

Output: src/steelseries/__init__.py, src/steelseries/driver.py, tests/test_steelseries_driver.py
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-steelseries-hid-backend/05-RESEARCH.md
@.planning/phases/05-steelseries-hid-backend/05-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/hidpp/features.py (BatteryResult contract — ss_battery_probe must return this):
```python
@dataclass
class BatteryResult:
    percent: int
    voltage_mv: int    # Set to 0 for SteelSeries
    charging: bool
    feature_used: str  # Convention: hex string e.g. "0xD2"
```

From src/hidpp/receiver.py (structural analog for find_dongle/open_dongle):
```python
LOGITECH_VID = 0x046D
VENDOR_USAGE_PAGE = 0xFF43
DEVICE_IDX = 0xFF

def find_receiver(vid: int = LOGITECH_VID, verbose: bool = True) -> list[dict]:
    all_devices = hid.enumerate(vid, 0)
    vendor_interfaces = [d for d in all_devices if d["usage_page"] == VENDOR_USAGE_PAGE]
    return vendor_interfaces

def open_receiver(info: dict):
    device = hid.device()
    device.open_path(info["path"])
    return device
```

From tests/conftest.py (mock_hid fixture — reuse as-is in test_steelseries_driver.py):
```python
@pytest.fixture
def mock_hid(mocker):
    device = mocker.MagicMock()
    device.write.return_value = None
    return device
```

From tests/test_receiver.py (test structure analog):
```python
def test_find_receiver_filters_ff43_only():
    with patch("hid.enumerate") as mock_enum:
        mock_enum.return_value = [{"usage_page": 0xFF00, ...}, {"usage_page": 0xFF43, ...}]
        result = find_receiver()
    assert len(result) == 1
    assert result[0]["usage_page"] == 0xFF43

def test_open_receiver_calls_open_path():
    with patch("hid.device") as mock_dev_class:
        mock_dev = MagicMock()
        mock_dev_class.return_value = mock_dev
        open_receiver({"path": b"/dev/hidraw0"})
    mock_dev.open_path.assert_called_once_with(b"/dev/hidraw0")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/steelseries/driver.py with find_dongle, open_dongle, ss_battery_probe</name>
  <files>src/steelseries/__init__.py, src/steelseries/driver.py</files>
  <behavior>
    - find_dongle(vid=SS_VID, verbose=False) calls hid.enumerate(vid, 0) and returns only entries where interface_number == SS_VENDOR_INTERFACE (3). When verbose=True, prints each interface with PID, interface_number, usage_page. Returns empty list if nothing matches.
    - open_dongle(info: dict) creates hid.device(), calls open_path(info["path"]), returns the device. Never calls hid.open(vid, pid).
    - ss_battery_probe(device, dev_idx: int) -> BatteryResult | None: issues exactly _SS_WARMUP_READS (3) device.read(64, timeout_ms=100) calls. Then writes [0x00, _SS_BATTERY_CMD]. Then reads up to _SS_MAX_RESPONSE_READS (20) packets; if resp is empty/falsy, breaks. If resp[0] == _SS_BATTERY_CMD (0xD2): raw = resp[1] & 0x7F; returns BatteryResult(percent=(raw-1)*5 if raw > 0 else 0, voltage_mv=0, charging=bool(resp[1] & 0x80), feature_used="0xD2"). If no 0xD2 response after 20 reads, returns None. dev_idx accepted for API contract but unused in command bytes.
    - Constants: SS_VID=0x1038, SS_AEROX5_PID=0x1852, SS_VENDOR_INTERFACE=3, SS_DEVICE_IDX=0x00, _SS_BATTERY_CMD=0xD2, _SS_WARMUP_READS=3, _SS_MAX_RESPONSE_READS=20
    - __init__.py is empty (package marker only)
  </behavior>
  <read_first>
    - src/hidpp/receiver.py (structural analog for find_dongle/open_dongle)
    - src/hidpp/features.py (BatteryResult dataclass to import)
    - .planning/phases/05-steelseries-hid-backend/05-RESEARCH.md (Pattern 1, Pattern 2, Code Examples section)
    - .planning/phases/05-steelseries-hid-backend/05-PATTERNS.md (driver.py pattern assignments section)
  </read_first>
  <action>
    Create src/steelseries/__init__.py as an empty file. Create src/steelseries/driver.py with the following structure:

    Module docstring explaining: SteelSeries Aerox 5 Wireless 2.4GHz dongle driver. Interface selection: filter by interface_number==3 (not usage_page). Handle lifecycle: open_path + close per call to ss_battery_probe — dongle responds to battery command once per connection.

    Imports: import hid; from hidpp.features import BatteryResult

    Constants block: SS_VID=0x1038, SS_AEROX5_PID=0x1852, SS_VENDOR_INTERFACE=3 (interface_number, NOT usage_page), SS_DEVICE_IDX=0x00 (placeholder for _open key; unused in command payload), _SS_BATTERY_CMD=0xD2 (= 0x92 | 0x40 wireless flag per rivalcfg), _SS_WARMUP_READS=3, _SS_MAX_RESPONSE_READS=20.

    find_dongle(vid: int = SS_VID, verbose: bool = False) -> list[dict]: enumerate all HID interfaces for vid using hid.enumerate(vid, 0). If verbose, print each interface showing PID, interface_number, usage_page (same format as find_receiver verbose output). Return only entries where d["interface_number"] == SS_VENDOR_INTERFACE.

    open_dongle(info: dict): Create hid.device(), call open_path(info["path"]), return device. Include docstring: "Open via open_path(). Never hid.open(vid, pid)."

    ss_battery_probe(device, dev_idx: int) -> "BatteryResult | None": Docstring must state that dev_idx is accepted for DEVICE_PROBES API compatibility but is unused (SteelSeries protocol is single-device). Warmup: loop _SS_WARMUP_READS times calling device.read(64, timeout_ms=100). Write: device.write([0x00, _SS_BATTERY_CMD]). Read loop: for _ in range(_SS_MAX_RESPONSE_READS): resp = device.read(64, timeout_ms=100); if not resp: break; if resp[0] == _SS_BATTERY_CMD: parse and return BatteryResult. After loop: return None.

    Anti-pattern guard: Do NOT call hid.open(vid, pid) anywhere in driver.py. Do NOT filter by usage_page. Do NOT reuse handles across calls.
  </action>
  <verify>
    <automated>cd F:\Cursor\batteryChecker && .venv-1\Scripts\python.exe -m pytest tests/test_steelseries_driver.py -x 2>&1 | tail -5</automated>
  </verify>
  <done>src/steelseries/__init__.py and src/steelseries/driver.py created; pytest for steelseries_driver passes (test file created in Task 2 first)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/test_steelseries_driver.py</name>
  <files>tests/test_steelseries_driver.py</files>
  <behavior>
    - test_find_dongle_filters_interface_3: patch hid.enumerate to return one iface with interface_number=0 and one with interface_number=3; assert find_dongle() returns only the iface with interface_number=3.
    - test_find_dongle_verbose_prints(capsys): patch hid.enumerate; call find_dongle(verbose=True); assert captured.out is not empty.
    - test_find_dongle_silent_when_not_verbose(capsys): call find_dongle(verbose=False); assert captured.out == "".
    - test_open_dongle_calls_open_path: patch hid.device; call open_dongle({"path": b"/dev/hid3"}); assert mock_dev.open_path.assert_called_once_with(b"/dev/hid3").
    - test_ss_battery_probe_parses_level_byte(mock_hid): mock_hid.read.side_effect = [[], [], [], [0xD2, 0x05] + [0x00]*62]; result = ss_battery_probe(mock_hid, 0x00); assert result.percent == 20, result.charging is False, result.voltage_mv == 0, result.feature_used == "0xD2".
    - test_ss_battery_probe_charging_flag(mock_hid): level_byte=0x85 (bit7 set, raw=5); assert result.percent == 20, result.charging is True.
    - test_ss_battery_probe_returns_none_on_timeout(mock_hid): mock_hid.read.return_value = []; assert ss_battery_probe(mock_hid, 0x00) is None.
    - test_ss_battery_probe_skips_0x61_packets(mock_hid): first responses are [0x61, ...] packets (at least 3), then [0xD2, 0x05, ...]; assert result.percent == 20 (0x61 packets skipped).
    - test_ss_battery_probe_warmup_read_count(mock_hid): mock_hid.read.side_effect = [[], [], [], [0xD2, 0x05]+[0]*62]; call ss_battery_probe(mock_hid, 0x00); count read calls before write — exactly 3 warmup reads issued before device.write().
    - test_no_hid_open_direct_call: AST-walk src/steelseries/driver.py; fail if any Call node is hid.open(...) (same pattern as TestSafetyInvariants in test_service.py).
    - test_raw_zero_gives_zero_percent(mock_hid): level_byte=0x01 (raw=1); assert percent == 0. level_byte=0x15 (raw=21); assert percent == 100.
  </behavior>
  <read_first>
    - tests/conftest.py (mock_hid fixture — reuse as-is; do not redeclare it)
    - tests/test_receiver.py (test structure analog: patch hid.enumerate pattern, open_path pattern)
    - tests/test_features.py (probe function test patterns)
    - tests/test_service.py (TestSafetyInvariants AST-walk pattern for hid.open check)
    - src/steelseries/driver.py (the module under test, written in Task 1)
  </read_first>
  <action>
    Create tests/test_steelseries_driver.py. Module docstring: "Unit tests for steelseries.driver."

    Imports: from unittest.mock import MagicMock, patch; import ast, pathlib; import pytest; from steelseries.driver import find_dongle, open_dongle, ss_battery_probe, SS_VENDOR_INTERFACE.

    The mock_hid fixture is in conftest.py — do NOT redeclare it. Import it implicitly via pytest fixture injection.

    For test_ss_battery_probe_warmup_read_count: use mock_hid.read.side_effect. To verify warmup count, check mock_hid.read.call_count after the probe and that mock_hid.write was called after 3 reads. Use mock_hid.mock_calls ordering: read must appear 3 times before the first write call in mock_hid.mock_calls.

    For test_no_hid_open_direct_call: use ast.parse(pathlib.Path("src/steelseries/driver.py").read_text()) and walk ast.Call nodes looking for Attribute nodes where .attr == "open" and .value.id == "hid"; pytest.fail if found.

    For test_ss_battery_probe_skips_0x61_packets: configure read.side_effect with 3 warmup empties, then multiple [0x61, 0x00, ...] packets, then the [0xD2, 0x05, ...] response. Total items in side_effect must be <= 3 + _SS_MAX_RESPONSE_READS (23). Confirm result.percent == 20.

    Use patch("hid.enumerate") and patch("hid.device") context managers (not mocker.patch) for the find_dongle and open_dongle tests since they don't use the mock_hid fixture.
  </action>
  <verify>
    <automated>cd F:\Cursor\batteryChecker && .venv-1\Scripts\python.exe -m pytest tests/test_steelseries_driver.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>All test_steelseries_driver.py tests pass green. Full suite still passes: pytest tests/ -x shows no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| USB HID → driver.py | Raw bytes from dongle; malformed responses possible if device is misbehaving or spoofed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | ss_battery_probe resp[1] parsing | mitigate | Mask with 0x7F before arithmetic; clamp result: pct = max(0, min(100, pct)) |
| T-05-02 | Denial of Service | find_dongle access denied on wrong interface | mitigate | Filter by interface_number=3; never open interface_number=0 (primary mouse — Windows locks it) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced in Phase 5 |
</threat_model>

<verification>
pytest tests/test_steelseries_driver.py -v — all tests green
pytest tests/ -x — no regressions in full suite (129+ tests)
python -c "from steelseries.driver import find_dongle, open_dongle, ss_battery_probe, SS_VID, SS_AEROX5_PID, SS_VENDOR_INTERFACE, SS_DEVICE_IDX" — imports without error
python -c "import ast, pathlib; tree=ast.parse(pathlib.Path('src/steelseries/driver.py').read_text()); [print('FAIL: hid.open found') for n in ast.walk(tree) if isinstance(n, ast.Call) and isinstance(getattr(n,'func',None), ast.Attribute) and n.func.attr=='open' and isinstance(n.func.value, ast.Name) and n.func.value.id=='hid']" — no output
</verification>

<success_criteria>
- src/steelseries/__init__.py exists (empty package marker)
- src/steelseries/driver.py exports find_dongle, open_dongle, ss_battery_probe, SS_VID, SS_AEROX5_PID, SS_VENDOR_INTERFACE, SS_DEVICE_IDX
- ss_battery_probe filters interface_number==3, NOT usage_page
- ss_battery_probe: 3 warmup reads before write, scans up to 20 response packets, skips 0x61 packets, parses level_byte per rivalcfg formula
- ss_battery_probe returns BatteryResult with voltage_mv=0 and feature_used="0xD2"
- ss_battery_probe returns None on timeout (no 0xD2 in 20 packets)
- No hid.open(vid,pid) call anywhere in driver.py (AST-verified)
- All test_steelseries_driver.py tests pass
- Full test suite (pytest tests/) passes with no regressions
</success_criteria>

<output>
Create .planning/phases/05-steelseries-hid-backend/05-01-SUMMARY.md when done
</output>
