---
phase: 04-qt-ui-window-tray
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/ui/device_card.py
  - src/ui/main_window.py
  - tests/test_ui_device_card.py
  - tests/test_ui_main_window.py
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "Each monitored device appears as a card showing device name, battery % (large text), and ONLINE/OFFLINE/CHARGING status"
    - "The battery % text is colored per threshold: critical red (<=8%), warning amber (<=45%), normal teal, grey when offline"
    - "A device's first DeviceState creates its card; subsequent states update the same card in place (no duplicates)"
    - "An OFFLINE state greys out / disconnects-indicates the card"
  artifacts:
    - path: "src/ui/device_card.py"
      provides: "DeviceCard(QFrame) with update_state(DeviceState)"
      contains: "def update_state"
    - path: "src/ui/main_window.py"
      provides: "on_device_update implemented — create-or-update card keyed by (vid,pid,dev_idx)"
  key_links:
    - from: "src/ui/main_window.py on_device_update"
      to: "src/ui/device_card.py DeviceCard.update_state"
      via: "dict lookup by (vid,pid,dev_idx); create+add to dashboard_layout if absent"
      pattern: "DeviceCard"
    - from: "src/ui/device_card.py"
      to: "ui.settings_manager.battery_color"
      via: "percent -> hex color for the % label"
      pattern: "battery_color"
---

<objective>
Implement the live device display (UI-01): a `DeviceCard(QFrame)` widget rendering device name,
battery percentage as large text, charging indicator, and an ONLINE/OFFLINE/CHARGING status
label/dot — colored by the D-01 threshold ladder. Wire `MainWindow.on_device_update` (the Wave 2
stub) to create-or-update cards keyed by `(vid, pid, dev_idx)` so the Dashboard populates live as
DeviceState snapshots arrive from the queue drain.

Purpose: Close the visible-data loop — this is the requirement (UI-01) the whole phase exists for.
Uses QFrame (a concrete QWidget subclass) deliberately so the QSS background renders WITHOUT the
paintEvent workaround required for raw QWidget subclasses (RESEARCH Pitfall 1).
Output: device_card.py, the implemented on_device_update in main_window.py, and unit tests for
card rendering, threshold colors, and create-vs-update behavior.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-qt-ui-window-tray/04-CONTEXT.md
@.planning/phases/04-qt-ui-window-tray/04-RESEARCH.md
@.planning/phases/04-qt-ui-window-tray/04-02-SUMMARY.md
@CLAUDE.md

<read_first>
- 04-RESEARCH.md Pattern 7 (battery_color), Pitfall 1 (QWidget paintEvent — why QFrame avoids it), Pitfall 6 (card not created on first update)
- 04-CONTEXT.md D-02 (card anatomy: name large, % large text, status dot/label, charging indicator — NO gauge), D-01 (threshold colors), code_context (lookup key (vid,pid,dev_idx); OFFLINE greys out)
- src/ui/main_window.py (Wave 2 — DashboardPage layout hook self.dashboard_layout and self._cards dict; on_device_update stub to implement)
- src/ui/settings_manager.py (battery_color helper from Wave 1)
- src/monitor/state.py (DeviceState fields, DeviceStatus enum)
- tests/test_ui_main_window.py (Wave 2 tests to extend, not break)
</read_first>

<interfaces>
<!-- From src/monitor/state.py -->
@dataclass(frozen=True)
class DeviceState:
    vid: int; pid: int; dev_idx: int; device_name: str
    percent: int | None; charging: bool; status: DeviceStatus
class DeviceStatus(enum.Enum): ONLINE; OFFLINE; CHARGING

<!-- From src/ui/settings_manager.py (Wave 1) -->
battery_color(percent: int | None) -> str   # "#888888"/"#E50000"/"#E5A300"/"#4FC3F7"

<!-- From src/ui/main_window.py (Wave 2) -->
self.dashboard_layout  # QVBoxLayout of the Dashboard scroll content (stretch at bottom)
self._cards: dict[tuple[int,int,int], object]  # empty, ready for population
def on_device_update(self, state: DeviceState) -> None  # STUB to implement here
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DeviceCard widget (UI-01)</name>
  <files>src/ui/device_card.py, tests/test_ui_device_card.py</files>
  <read_first>
    - 04-RESEARCH.md Pattern 7, Pitfall 1
    - 04-CONTEXT.md D-01, D-02
  </read_first>
  <behavior>
    - DeviceCard(state).update_state(state) renders state.device_name in a name label
    - the percent label shows "<n>%" when percent is not None, and a placeholder ("--" or "Offline") when None
    - the percent label stylesheet color equals battery_color(state.percent) for that percent
    - the status label text equals state.status.name (ONLINE / OFFLINE / CHARGING)
    - when state.charging is True, a charging indicator is shown (e.g. status reads CHARGING or a charging glyph/label is visible)
    - when state.status is OFFLINE the card is visually muted (e.g. an objectName/property "offline" set true, or reduced opacity) — assert the muted flag/property, not pixels
  </behavior>
  <action>
    Create src/ui/device_card.py with DeviceCard(QFrame) — use QFrame (concrete subclass) so QSS
    background applies without the paintEvent workaround (Pitfall 1).
      - __init__(self, state: DeviceState | None = None, parent=None): build child widgets:
          self._name = QLabel (large/title font),
          self._percent = QLabel (large prominent "value" font per design typography),
          self._status = QLabel (status text/dot),
          optional charging indicator (a QLabel or icon toggled visible).
        Lay them out (QVBoxLayout or grid — Claude's discretion; D-02 ordering: name, %, status).
        Give the frame an objectName (e.g. "deviceCard") for QSS targeting. setProperty for offline state.
        If state is provided, call self.update_state(state).
      - update_state(self, state: DeviceState) -> None:
          set name text from state.device_name (use this, not hid product_string — matches KNOWN_DEVICES).
          set percent label: f"{state.percent}%" if not None else "--" (or "Offline").
          set percent label color via setStyleSheet(f"color: {battery_color(state.percent)};") — import battery_color from ui.settings_manager.
          set status label to state.status.name.
          show/hide the charging indicator from state.charging (and/or reflect CHARGING status).
          set the offline muted property: self.setProperty("offline", state.status == DeviceStatus.OFFLINE) then re-polish (style().unpolish/polish) so QSS picks it up, OR set a reduced opacity/disabled visual — Claude's discretion, but it must be assertable.
    Add card QSS rules to ui/styles.py DARK_QSS if needed (e.g. QFrame#deviceCard background #262355, rounded; QFrame#deviceCard[offline="true"] muted). Keep edits to styles.py minimal and additive.
    Write tests/test_ui_device_card.py (pytest-qt qapp): construct DeviceState fixtures for ONLINE 80%, warning 30%, critical 5%, OFFLINE None, CHARGING; assert every <behavior> bullet via label text and the stylesheet color substring (battery_color(percent) hex present in the percent label stylesheet).
  </action>
  <verify>
    <automated>.venv-1\Scripts\pytest tests/test_ui_device_card.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - name, percent, and status render from DeviceState
    - percent label color matches battery_color(percent) for critical/warning/normal/None
    - None percent shows a placeholder, not "None%"
    - OFFLINE state sets an assertable muted flag/property
    - charging True surfaces a charging indicator
  </acceptance_criteria>
  <done>DeviceCard renders the full D-02 card anatomy with threshold-colored % and offline muting, fully unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire on_device_update — live create-or-update (UI-01)</name>
  <files>src/ui/main_window.py, tests/test_ui_main_window.py</files>
  <read_first>
    - 04-RESEARCH.md Pitfall 6 (must create card on first state)
    - 04-CONTEXT.md code_context (lookup key (vid,pid,dev_idx))
    - src/ui/main_window.py (the stub on_device_update + dashboard_layout + _cards from Wave 2)
  </read_first>
  <behavior>
    - on_device_update with a never-seen (vid,pid,dev_idx) creates a DeviceCard, stores it in self._cards under that key, and inserts it into the dashboard layout
    - on_device_update with a previously-seen key does NOT create a second card — it calls update_state on the existing card
    - after two updates for the same device, len(self._cards) == 1 and the card reflects the latest state
    - two different devices produce two distinct cards (len(self._cards) == 2)
  </behavior>
  <action>
    Implement MainWindow.on_device_update(self, state: DeviceState) -> None, replacing the Wave 2 stub:
      - key = (state.vid, state.pid, state.dev_idx).
      - if key not in self._cards: create DeviceCard(state), self._cards[key] = card, insert into
        self.dashboard_layout BEFORE the trailing stretch (use insertWidget at count-1, or track an
        index) so cards stack top-down and the stretch stays last.
      - else: self._cards[key].update_state(state).
      - This runs on the Qt main thread only (called by the QTimer drain) — no locking needed.
    Import DeviceCard from ui.device_card at the top of main_window.py (now safe — Wave 3 owns this edit).
    Extend tests/test_ui_main_window.py (do NOT break Wave 2 tests): assert first-update creates a
    card (len==1), repeat-update keeps len==1 and updates in place, distinct device creates a second
    card (len==2). Build DeviceState fixtures inline.
  </action>
  <verify>
    <automated>.venv-1\Scripts\pytest tests/test_ui_main_window.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - first state for a key creates exactly one card and adds it to the dashboard layout
    - repeated states for the same key update the existing card (no duplicate; len stays 1)
    - two distinct devices yield two cards
    - existing Wave 2 MainWindow tests still pass (closeEvent, 5-page stack)
  </acceptance_criteria>
  <done>Dashboard populates and updates live from DeviceState snapshots, keyed by (vid,pid,dev_idx), with no duplicate cards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| queue.Queue (bg thread) -> on_device_update (main thread) | DeviceState snapshots cross here via the QTimer drain; the card logic must only mutate widgets on the main thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Denial of Service | First DeviceState dropped because card-create branch missing (Pitfall 6) | mitigate | on_device_update has explicit create-if-absent branch; verified by first-update test |
| T-04-10 | Tampering | Duplicate cards from repeated states leaking widgets | mitigate | Keyed _cards dict ensures one card per (vid,pid,dev_idx); verified by repeat-update test |
| T-04-11 | Information Disclosure | Rendering hid product_string instead of curated KNOWN_DEVICES name | accept | DeviceState.device_name already comes from KNOWN_DEVICES (Phase 3 contract); card uses state.device_name directly |
</threat_model>

<verification>
- `.venv-1\Scripts\pytest tests/test_ui_device_card.py tests/test_ui_main_window.py -x -q` green
- `.venv-1\Scripts\pytest tests/ -x -q` full suite green
- create-vs-update and threshold-color behaviors covered by unit tests
</verification>

<success_criteria>
- DeviceCard shows name, %, status, charging per D-02 with threshold colors per D-01 (UI-01)
- on_device_update creates cards on first sight and updates them thereafter, keyed by (vid,pid,dev_idx)
- OFFLINE devices are visibly muted
- No duplicate cards; existing tests unbroken
</success_criteria>

<output>
Create `.planning/phases/04-qt-ui-window-tray/04-03-SUMMARY.md` when done.
</output>
